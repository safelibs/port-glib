name: port-glib-safe-rust-workflow
backend: codex
working_dir: /home/yans/safelibs/pipeline/ports/port-glib
max_bounces: 50
linear: true
yaml_source_mode: inline-only
verifier_encoding: explicit-phases
vars:
  GOAL: Complete the GLib 2.80.0 SafeLibs Rust port in safe/ so it is source-compatible, link-compatible, runtime-compatible, package-compatible, and validated on Ubuntu 24.04 while consuming the existing local artifacts in place.
phases:
  - id: impl-safe-bootstrap
    type: implement
    bounce_target: null
    prompt: |
      Role: Implementer for phase `impl-safe-bootstrap`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `1`.
      - Phase ID: `impl-safe-bootstrap`.
      - Phase type: `implement`.
      - Fixed top-level `bounce_target`: `null`.
      - Verifier phases that immediately follow this implement block, in order: `check-safe-bootstrap-metadata`, `check-safe-bootstrap-abi-shell`.
      - Do not invent, rename, reorder, merge, or split phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or any YAML-source indirection.
      - Do not introduce agent-selected `bounce_targets`; every verifier has the fixed `bounce_target` declared in this workflow.

      Preexisting Inputs
      Consume or update these existing local artifacts in place. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/`
      - `dependents.json`
      - `relevant_cves.json`
      - `safe/Cargo.toml`
      - `safe/meson.build`
      - `safe/debian/rules`
      - `safe/debian/`
      - `safe/vendor/original/`
      - `safe/vendor/build-check/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/abi/link-compat/*.json`
      - `safe/abi/layout-manifests/*.json`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/debian-patches.json`
      - `safe/tests/upstream/*`
      - `safe/tests/manifests/*`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/debian-patch-provenance.md`
      - `test-original.sh`

      New Outputs
      Produce the phase outputs below, plus one git commit containing this phase's work before yielding. Do not leave required deliverables only in the worktree.
      - A verified, stable baseline for all later phases.
      - An explicit editable-mirror contract for `safe/tests/upstream/*`.
      - Refreshed bootstrap manifests only if the contract changes in place.

      Artifact Handling Rules
      - Work from the current repository root and follow `AGENTS.md`.
      - Preserve unrelated user work; only edit files needed for this phase.
      - Consume or update existing artifacts instead of replacing them with fresh upstream fetches or newly rediscovered inventories.
      - If a listed artifact is stale, update it in place only when the phase source explicitly requires that change.
      - Commit the completed phase work to git before yielding to the verifier phases.

      Phase Source
      # Bootstrap the ABI Shell and Artifact Contract

      ## Phase Name
      Bootstrap the ABI shell and artifact contract

      ## Implement Phase ID
      `impl-safe-bootstrap`

      ## Source Plan Context
      - Overall target: turn upstream GLib 2.80.0 in `original/` into a Rust implementation in `safe/` that is source-compatible, link-compatible, runtime-compatible, and package-compatible on Ubuntu 24.04.
      - Current bootstrap state: `safe/` already has a Cargo workspace for `glib`, `gthread`, `gmodule`, `gobject`, `gio`, `girepository`, and `abi-support`; a Meson bootstrap wrapper; a Debian package build path; frozen upstream/build artifacts; ABI, symbol, layout, package, and test manifests; editable upstream test mirrors; CVE probes; and package smoke tests.
      - Current Rust ownership is partial. `safe/crates/glib` already owns selected CVE-sensitive exports, `safe/tools/build-glib-backend.py` rewrites 96 frozen GLib compile commands into a replayed backend, `gthread`, `gmodule`, and `gio` still inject upstream objects, and `safe/tools/build-abi-shell.py` still seeds from `safe/vendor/build-check` and fabricates static archives for several libraries.
      - GObject is mostly translated Rust but still warning-heavy and not ready for the final unsafe audit. GIRepository currently has 229 placeholder exports plus 2 real exports. `test-original.sh` is still an original-GLib harness rather than a safe-package harness.
      - Upstream export counts from `safe/abi/version-scripts/*.map`: `libglib-2.0.so.0` has 1872 symbols, `libgthread-2.0.so.0` has 2, `libgmodule-2.0.so.0` has 10, `libgobject-2.0.so.0` has 478, `libgio-2.0.so.0` has 2107, and `libgirepository-2.0.so.0` has 231.
      - Frozen test manifest counts in `safe/tests/manifests/`: `glib-core.txt` has 83 rows, `glib-advanced.txt` has 68, `gobject.txt` has 62, `gio.txt` has 133, `girepository.txt` has 11, `fuzzing.txt` has 22, and `full.txt` has 384.
      - The existing hybrid bootstrap is an oracle and compatibility scaffold only. The final implementation must retire direct linkage to upstream object files, vendored static-archive construction, and shipped helper/tool artifacts copied from `safe/vendor/build-check`.

      ## Preexisting Inputs
      - `original/`
      - `dependents.json`
      - `relevant_cves.json`
      - `safe/Cargo.toml`
      - `safe/meson.build`
      - `safe/debian/rules`
      - `safe/debian/`
      - `safe/vendor/original/`
      - `safe/vendor/build-check/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/abi/link-compat/*.json`
      - `safe/abi/layout-manifests/*.json`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/debian-patches.json`
      - `safe/tests/upstream/*`
      - `safe/tests/manifests/*`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/debian-patch-provenance.md`
      - `test-original.sh`

      ## New Outputs
      - A verified, stable baseline for all later phases.
      - An explicit editable-mirror contract for `safe/tests/upstream/*`.
      - Refreshed bootstrap manifests only if the contract changes in place.

      ## File Changes
      - `safe/tools/sync-upstream-assets.py`
      - `safe/abi/test-source-path-map.json`
      - `safe/tests/upstream/glib/meson.build`
      - `safe/tests/upstream/gobject/meson.build`
      - `safe/tests/upstream/gmodule/meson.build`
      - `safe/tests/upstream/gthread/meson.build`
      - `safe/tests/upstream/glib/markup-collect.c`
      - Any `safe/abi/*` manifest whose current meaning changes because of the editable-mirror contract

      ## Implementation Details
      - Generated workflow contract for the whole phase set:
        - Keep execution linear; do not introduce `parallel_groups`.
        - Emit inline YAML only; do not use top-level `include` or phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or any other YAML-source indirection.
        - Model every verifier as an explicit top-level `check` phase with exactly one fixed `bounce_target`.
        - Keep each verifier immediately in the implementation block it verifies, and bounce only to that implement phase.
        - Put every verifier command directly in that checker's instructions; do not model command execution as separate non-agentic phases.
        - Keep each major implementation phase as one implement phase followed only by the listed check phases.
        - Require every implement phase to commit at least once before yielding; checkers must fail unchanged `HEAD` or worktree-only deliverables.
      - Preserve `safe/vendor/original/` and `safe/vendor/build-check/` as authoritative read-only inputs.
      - Preserve the consume-existing-artifacts contract for `original/`, `dependents.json`, `relevant_cves.json`, `safe/vendor/original/`, `safe/vendor/build-check/`, `safe/abi/`, `safe/tests/manifests/`, `safe/tests/upstream/`, `safe/debian/`, `safe/docs/cve-matrix.md`, `safe/docs/debian-patch-provenance.md`, and `test-original.sh`.
      - Fix the current mismatch where `python3 tools/sync-upstream-assets.py --verify-map abi/test-source-path-map.json` fails because some editable mirrors intentionally diverge from the vendored originals.
      - Change the mirror-verification contract so it verifies the path-map shape remains fixed, the editable trees still correspond to the vendored source roots, and intentional local edits are explicit and preserved in place.
      - Keep the current local mirror edits that already encode safe-specific test expectations.
      - `safe/tests/upstream/glib/markup-collect.c` uses `"GLib"` as the expected critical-log domain instead of `G_LOG_DOMAIN`.
      - `safe/tests/upstream/gobject/meson.build` already raises the `closure-refcount` timeout.
      - `safe/tests/upstream/*/meson.build` files already document that these trees are canonical editable mirrors for replay.
      - Remove `__pycache__` noise from the editable-mirror contract; these should not control the plan.
      - Do not widen or regenerate the upstream artifact set. Later phases must consume the current `safe/abi/*` and `safe/tests/manifests/*` files in place.
      - Critical file responsibilities owned by this phase:
        - `safe/Cargo.toml` remains the workspace membership and crate-graph source for all Rust libraries.
        - `safe/meson.build` remains the Meson-side bootstrap entry point for Cargo and ABI-shell builds.
        - `safe/tools/extract_abi.py`, `safe/tools/extract_layouts.py`, `safe/tools/link-compat.py`, and `safe/tools/run-meson-manifest.py` remain the canonical ABI, layout, link/run, and upstream-test verification tools.
        - `safe/tools/sync-upstream-assets.py` becomes the editable-mirror contract verifier instead of a byte-for-byte resync tool.
        - `safe/abi/tests.json`, `safe/abi/test-source-path-map.json`, `safe/abi/link-compat/*.json`, `safe/abi/layout-manifests/*.json`, and `safe/tests/manifests/*.txt` are consumed and updated only in place.
        - `safe/tests/upstream/*` remain canonical editable copies of upstream tests used by manifest-driven replay.

      ## Verification Phases
      ### `check-safe-bootstrap-metadata`
      - Phase ID: `check-safe-bootstrap-metadata`
      - Type: `check`
      - Fixed `bounce_target`: `impl-safe-bootstrap`
      - Purpose: Verify the frozen metadata, phase manifests, and editable-mirror contract before deeper porting begins.
      - Commands:
      ```bash
      cd safe
      cargo check --workspace
      python3 tools/extract_abi.py --verify
      python3 tools/extract_layouts.py --verify
      python3 tools/link-compat.py --verify-manifests
      python3 tools/verify-debian-patches.py --verify-manifest
      ```

      ### `check-safe-bootstrap-abi-shell`
      - Phase ID: `check-safe-bootstrap-abi-shell`
      - Type: `check`
      - Fixed `bounce_target`: `impl-safe-bootstrap`
      - Purpose: Verify that the current Rust workspace still builds an ABI shell that links original objects and packages correctly.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-abi-shell --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-abi-shell/.stamp
      python3 tools/link-compat.py --phase abi-shell --build-root build-abi-shell --compile-original-objects --run
      dpkg-buildpackage -b -uc -us
      python3 tools/verify-package-baselines.py --source . --work-root build-package-baselines --abi-shell-profiles "nodoc noinsttest nogir noudeb" --install-manifests abi/install-manifests --postinst-manifest abi/postinst-state/runtime.json
      ```

      ## Success Criteria
      - `check-safe-bootstrap-metadata` and `check-safe-bootstrap-abi-shell` both pass.
      - No later phase depends on a full resync of `safe/tests/upstream/*`.

      ## Git Commit Requirement
      - The implementer must commit work to git before yielding.
      - This phase must produce at least one new git commit before yielding to its verification phases.
      - A verifier must treat an unchanged `HEAD` or a worktree-only deliverable as a failure.
  - id: check-safe-bootstrap-metadata
    type: check
    bounce_target: impl-safe-bootstrap
    prompt: |
      Reviewer role: You are the checker for phase `check-safe-bootstrap-metadata`, reviewing the work produced by `impl-safe-bootstrap`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `2`.
      - Phase ID: `check-safe-bootstrap-metadata`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-safe-bootstrap`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/`
      - `dependents.json`
      - `relevant_cves.json`
      - `safe/Cargo.toml`
      - `safe/meson.build`
      - `safe/debian/rules`
      - `safe/debian/`
      - `safe/vendor/original/`
      - `safe/vendor/build-check/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/abi/link-compat/*.json`
      - `safe/abi/layout-manifests/*.json`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/debian-patches.json`
      - `safe/tests/upstream/*`
      - `safe/tests/manifests/*`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/debian-patch-provenance.md`
      - `test-original.sh`
      - `safe/tools/extract_abi.py`
      - `safe/tools/extract_layouts.py`
      - `safe/tools/link-compat.py`
      - `safe/tools/verify-debian-patches.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-safe-bootstrap`.
      - Verify `impl-safe-bootstrap` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-safe-bootstrap` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-safe-bootstrap`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      cargo check --workspace
      python3 tools/extract_abi.py --verify
      python3 tools/extract_layouts.py --verify
      python3 tools/link-compat.py --verify-manifests
      python3 tools/verify-debian-patches.py --verify-manifest
      ```

      Phase Check Source
      ### `check-safe-bootstrap-metadata`
      - Phase ID: `check-safe-bootstrap-metadata`
      - Type: `check`
      - Fixed `bounce_target`: `impl-safe-bootstrap`
      - Purpose: Verify the frozen metadata, phase manifests, and editable-mirror contract before deeper porting begins.
      - Commands:
      ```bash
      cd safe
      cargo check --workspace
      python3 tools/extract_abi.py --verify
      python3 tools/extract_layouts.py --verify
      python3 tools/link-compat.py --verify-manifests
      python3 tools/verify-debian-patches.py --verify-manifest
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-safe-bootstrap-abi-shell
    type: check
    bounce_target: impl-safe-bootstrap
    prompt: |
      Reviewer role: You are the checker for phase `check-safe-bootstrap-abi-shell`, reviewing the work produced by `impl-safe-bootstrap`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `3`.
      - Phase ID: `check-safe-bootstrap-abi-shell`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-safe-bootstrap`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `safe/Cargo.toml`
      - `safe/meson.build`
      - `safe/debian/rules`
      - `safe/vendor/original/`
      - `safe/vendor/build-check/`
      - `safe/abi/link-compat/*.json`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/postinst-state/runtime.json`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/link-compat.py`
      - `safe/tools/verify-package-baselines.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-safe-bootstrap`.
      - Verify `impl-safe-bootstrap` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-safe-bootstrap` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-safe-bootstrap`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-abi-shell --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-abi-shell/.stamp
      python3 tools/link-compat.py --phase abi-shell --build-root build-abi-shell --compile-original-objects --run
      dpkg-buildpackage -b -uc -us
      python3 tools/verify-package-baselines.py --source . --work-root build-package-baselines --abi-shell-profiles "nodoc noinsttest nogir noudeb" --install-manifests abi/install-manifests --postinst-manifest abi/postinst-state/runtime.json
      ```

      Phase Check Source
      ### `check-safe-bootstrap-abi-shell`
      - Phase ID: `check-safe-bootstrap-abi-shell`
      - Type: `check`
      - Fixed `bounce_target`: `impl-safe-bootstrap`
      - Purpose: Verify that the current Rust workspace still builds an ABI shell that links original objects and packages correctly.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-abi-shell --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-abi-shell/.stamp
      python3 tools/link-compat.py --phase abi-shell --build-root build-abi-shell --compile-original-objects --run
      dpkg-buildpackage -b -uc -us
      python3 tools/verify-package-baselines.py --source . --work-root build-package-baselines --abi-shell-profiles "nodoc noinsttest nogir noudeb" --install-manifests abi/install-manifests --postinst-manifest abi/postinst-state/runtime.json
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl-glib-core
    type: implement
    bounce_target: null
    prompt: |
      Role: Implementer for phase `impl-glib-core`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `4`.
      - Phase ID: `impl-glib-core`.
      - Phase type: `implement`.
      - Fixed top-level `bounce_target`: `null`.
      - Verifier phases that immediately follow this implement block, in order: `check-glib-core-link`, `check-glib-core-tests`.
      - Do not invent, rename, reorder, merge, or split phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or any YAML-source indirection.
      - Do not introduce agent-selected `bounce_targets`; every verifier has the fixed `bounce_target` declared in this workflow.

      Preexisting Inputs
      Consume or update these existing local artifacts in place. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `safe/Cargo.toml`
      - `safe/meson.build`
      - `safe/debian/rules`
      - `safe/vendor/original/`
      - `safe/vendor/build-check/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/abi/link-compat/*.json`
      - `safe/abi/layout-manifests/*.json`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/debian-patches.json`
      - `safe/tests/upstream/*`
      - `safe/tests/manifests/*`
      - `original/glib/*.c`
      - `original/glib/*.h`
      - `original/glib/tests/*`
      - `original/gthread/gthread-impl.c`
      - `original/gmodule/gmodule.c`
      - `original/gmodule/gmodule-deprecated.c`
      - `original/gmodule/gmodule.h`
      - `safe/crates/glib/`
      - `safe/crates/gthread/`
      - `safe/crates/gmodule/`
      - `safe/abi/link-compat/glib-core.json`
      - `safe/tests/manifests/glib-core.txt`
      - `safe/abi/layout-manifests/glib.json`
      - `safe/abi/layout-manifests/gthread.json`
      - `safe/abi/layout-manifests/gmodule.json`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`

      New Outputs
      Produce the phase outputs below, plus one git commit containing this phase's work before yielding. Do not leave required deliverables only in the worktree.
      - Pure-Rust `libgthread-2.0.so.0` / `libgthread-2.0.a` and `libgmodule-2.0.so.0` / `libgmodule-2.0.a`.
      - A greatly expanded Rust-owned `libglib-2.0.so.0` / `libglib-2.0.a` core surface.
      - Core ABI/layout parity verified from the safe build root.

      Artifact Handling Rules
      - Work from the current repository root and follow `AGENTS.md`.
      - Preserve unrelated user work; only edit files needed for this phase.
      - Consume or update existing artifacts instead of replacing them with fresh upstream fetches or newly rediscovered inventories.
      - If a listed artifact is stale, update it in place only when the phase source explicitly requires that change.
      - Commit the completed phase work to git before yielding to the verifier phases.

      Phase Source
      # Port Core GLib, GThread, and GModule

      ## Phase Name
      Port core GLib, GThread, and GModule

      ## Implement Phase ID
      `impl-glib-core`

      ## Source Plan Context
      - Overall target remains GLib 2.80.0 compatibility on Ubuntu 24.04 using the existing `original/`, `safe/vendor/original/`, `safe/vendor/build-check/`, `safe/abi/`, `safe/tests/manifests/`, and editable `safe/tests/upstream/` artifacts in place.
      - Relevant export counts: `libglib-2.0.so.0` has 1872 symbols, `libgthread-2.0.so.0` has 2, and `libgmodule-2.0.so.0` has 10.
      - Relevant frozen manifest: `safe/tests/manifests/glib-core.txt` has 83 rows.
      - Current bootstrap state for this phase: `safe/tools/build-glib-backend.py` still rewrites 96 frozen GLib compile commands, and `safe/crates/gthread/build.rs` plus `safe/crates/gmodule/build.rs` still inject upstream object files from `safe/vendor/build-check`.
      - This phase consumes the phase 1 artifact contract as an existing baseline and moves the core surface toward Rust ownership without rediscovering or regenerating upstream snapshots or frozen metadata.

      ## Preexisting Inputs
      - `safe/Cargo.toml`
      - `safe/meson.build`
      - `safe/debian/rules`
      - `safe/vendor/original/`
      - `safe/vendor/build-check/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/abi/link-compat/*.json`
      - `safe/abi/layout-manifests/*.json`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/debian-patches.json`
      - `safe/tests/upstream/*`
      - `safe/tests/manifests/*`
      - `original/glib/*.c`, `original/glib/*.h`, and `original/glib/tests/*`
      - `original/gthread/gthread-impl.c`
      - `original/gmodule/gmodule.c`, `original/gmodule/gmodule-deprecated.c`, `original/gmodule/gmodule.h`
      - `safe/abi/link-compat/glib-core.json`
      - `safe/tests/manifests/glib-core.txt`
      - `safe/abi/layout-manifests/glib.json`
      - `safe/abi/layout-manifests/gthread.json`
      - `safe/abi/layout-manifests/gmodule.json`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`

      ## New Outputs
      - Pure-Rust `libgthread-2.0.so.0` / `libgthread-2.0.a` and `libgmodule-2.0.so.0` / `libgmodule-2.0.a`.
      - A greatly expanded Rust-owned `libglib-2.0.so.0` / `libglib-2.0.a` core surface.
      - Core ABI/layout parity verified from the safe build root.

      ## File Changes
      - `safe/crates/glib/src/lib.rs`
      - `safe/crates/glib/src/base/mod.rs`
      - `safe/crates/glib/src/collections/mod.rs`
      - `safe/crates/glib/src/mainloop/mod.rs`
      - `safe/crates/glib/src/strings/mod.rs`
      - `safe/crates/glib/src/threading/mod.rs`
      - `safe/crates/glib/build.rs`
      - `safe/crates/glib/src/backend.rs`
      - `safe/crates/gthread/build.rs`
      - `safe/crates/gthread/src/lib.rs`
      - `safe/crates/gthread/src/runtime.rs`
      - `safe/crates/gthread/src/compat.rs`
      - `safe/crates/gmodule/build.rs`
      - `safe/crates/gmodule/src/lib.rs`
      - `safe/crates/gmodule/src/module_api.rs`
      - `safe/crates/gmodule/src/runtime.rs`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/build-glib-backend.py`
      - `safe/abi/version-scripts/libglib.map`
      - `safe/abi/version-scripts/libgthread.map`
      - `safe/abi/version-scripts/libgmodule.map`
      - `safe/abi/link-compat/glib-core.json`
      - `safe/abi/layouts/{glib,gthread,gmodule}.json` if layout baselines must expand
      - `safe/crates/abi-support/src/ffi.rs` if shared public ABI primitives must be extended
      - `safe/crates/abi-support/src/bin/layout-probe.rs` if layout probing must cover newly Rust-owned public ABI types

      ## Implementation Details
      - Consume the existing `original/`, `safe/vendor/original/`, `safe/vendor/build-check/`, `safe/abi/`, `safe/tests/manifests/`, and `safe/tests/upstream/` artifacts in place; do not refetch or regenerate them.
      - Replace the direct vendored-object strategy in `safe/crates/gthread/build.rs` and `safe/crates/gmodule/build.rs` with normal Rust-owned exports that preserve the upstream ABI and public symbol list.
      - Replace the vendored static-archive fallback in `safe/tools/build-abi-shell.py` for `gthread` and `gmodule`; phase completion requires `build-glib-core/gthread/libgthread-2.0.a` and `build-glib-core/gmodule/libgmodule-2.0.a` to come from Cargo/Rust outputs, not `safe/vendor/build-check`.
      - Port the data-structure and runtime surfaces exercised by `tests/manifests/glib-core.txt`: arrays, bytes, lists, queues, strings, async queues, atomics, rcbox/refcount, threading helpers, main-loop primitives, timers, and the core utility surface.
      - Keep the public layouts in `safe/abi/layout-manifests/{glib,gthread,gmodule}.json` exact; expand the manifests if additional public types become Rust-owned.
      - Extend `safe/crates/abi-support/src/ffi.rs` and `safe/crates/abi-support/src/bin/layout-probe.rs` only when core GLib, GThread, or GModule public ABI types become Rust-owned, and keep those definitions synchronized with the layout manifests.
      - Use the current `safe-glib` backend-replay machinery only as an incremental scaffold. The phase should move the core surface out of replayed C objects and into Rust code.
      - Keep the generated-export and version-script contracts stable so that link-compat rows in `safe/abi/link-compat/glib-core.json` continue to point at the same symbol names.
      - Preserve the upstream-installed helper binaries and pkg-config behavior needed by later phases.
      - Critical file responsibilities owned by this phase:
        - `safe/tools/build-abi-shell.py` remains the central build-root orchestrator for staged libraries, pkg-config files, helper binaries, intro-test inventory, and layout exports; this phase retires its `gthread` and `gmodule` vendored static-archive fallbacks.
        - `safe/abi/version-scripts/libglib.map`, `safe/abi/version-scripts/libgthread.map`, and `safe/abi/version-scripts/libgmodule.map` are the authoritative exported-symbol lists for this core surface.
        - `safe/crates/abi-support/src/ffi.rs` and `safe/crates/abi-support/src/bin/layout-probe.rs` hold shared ABI primitives and layout probing; extend them only with public ABI types that become Rust-owned.
        - `safe/crates/glib/src/lib.rs` remains the top-level GLib module map for core and later advanced GLib modules.
        - `safe/crates/gthread/*` and `safe/crates/gmodule/*` are small but critical libraries whose current build paths must stop injecting upstream objects.

      ## Verification Phases
      ### `check-glib-core-link`
      - Phase ID: `check-glib-core-link`
      - Type: `check`
      - Fixed `bounce_target`: `impl-glib-core`
      - Purpose: Prove that original objects compiled against the upstream public headers still link and run against the safe core libraries.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-core --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-core/.stamp
      python3 tools/link-compat.py --phase glib-core --build-root build-glib-core --compile-original-objects --run
      ```

      ### `check-glib-core-tests`
      - Phase ID: `check-glib-core-tests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-glib-core`
      - Purpose: Replay the frozen core upstream tests against the safe build.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-core --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-core/.stamp
      python3 tools/run-meson-manifest.py --build-root build-glib-core --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-glib-core/meson-info/intro-tests.json --manifest tests/manifests/glib-core.txt --print-errorlogs
      ```

      ## Success Criteria
      - `check-glib-core-link` and `check-glib-core-tests` both pass.
      - `safe/crates/gthread/build.rs` and `safe/crates/gmodule/build.rs` no longer add upstream `.o` files from `safe/vendor/build-check`.
      - `safe/tools/build-abi-shell.py` no longer builds `libgthread-2.0.a` or `libgmodule-2.0.a` from vendored object directories.

      ## Git Commit Requirement
      - The implementer must commit work to git before yielding.
      - This phase must produce at least one new git commit before yielding to its verification phases.
      - A verifier must treat an unchanged `HEAD` or a worktree-only deliverable as a failure.
  - id: check-glib-core-link
    type: check
    bounce_target: impl-glib-core
    prompt: |
      Reviewer role: You are the checker for phase `check-glib-core-link`, reviewing the work produced by `impl-glib-core`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `5`.
      - Phase ID: `check-glib-core-link`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-glib-core`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/glib/*.c`
      - `original/glib/*.h`
      - `original/glib/tests/*`
      - `original/gthread/gthread-impl.c`
      - `original/gmodule/gmodule.c`
      - `original/gmodule/gmodule-deprecated.c`
      - `original/gmodule/gmodule.h`
      - `safe/crates/glib/`
      - `safe/crates/gthread/`
      - `safe/crates/gmodule/`
      - `safe/abi/link-compat/glib-core.json`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/link-compat.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-glib-core`.
      - Verify `impl-glib-core` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-glib-core` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-glib-core`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-core --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-core/.stamp
      python3 tools/link-compat.py --phase glib-core --build-root build-glib-core --compile-original-objects --run
      ```

      Phase Check Source
      ### `check-glib-core-link`
      - Phase ID: `check-glib-core-link`
      - Type: `check`
      - Fixed `bounce_target`: `impl-glib-core`
      - Purpose: Prove that original objects compiled against the upstream public headers still link and run against the safe core libraries.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-core --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-core/.stamp
      python3 tools/link-compat.py --phase glib-core --build-root build-glib-core --compile-original-objects --run
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-glib-core-tests
    type: check
    bounce_target: impl-glib-core
    prompt: |
      Reviewer role: You are the checker for phase `check-glib-core-tests`, reviewing the work produced by `impl-glib-core`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `6`.
      - Phase ID: `check-glib-core-tests`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-glib-core`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/glib/*.c`
      - `original/glib/*.h`
      - `original/glib/tests/*`
      - `safe/crates/glib/`
      - `safe/crates/gthread/`
      - `safe/crates/gmodule/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/tests/manifests/glib-core.txt`
      - `safe/tests/upstream/*`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/run-meson-manifest.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-glib-core`.
      - Verify `impl-glib-core` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-glib-core` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-glib-core`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-core --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-core/.stamp
      python3 tools/run-meson-manifest.py --build-root build-glib-core --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-glib-core/meson-info/intro-tests.json --manifest tests/manifests/glib-core.txt --print-errorlogs
      ```

      Phase Check Source
      ### `check-glib-core-tests`
      - Phase ID: `check-glib-core-tests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-glib-core`
      - Purpose: Replay the frozen core upstream tests against the safe build.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-core --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-core/.stamp
      python3 tools/run-meson-manifest.py --build-root build-glib-core --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-glib-core/meson-info/intro-tests.json --manifest tests/manifests/glib-core.txt --print-errorlogs
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl-glib-advanced
    type: implement
    bounce_target: null
    prompt: |
      Role: Implementer for phase `impl-glib-advanced`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `7`.
      - Phase ID: `impl-glib-advanced`.
      - Phase type: `implement`.
      - Fixed top-level `bounce_target`: `null`.
      - Verifier phases that immediately follow this implement block, in order: `check-glib-advanced-link`, `check-glib-advanced-tests`, `check-glib-cves`.
      - Do not invent, rename, reorder, merge, or split phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or any YAML-source indirection.
      - Do not introduce agent-selected `bounce_targets`; every verifier has the fixed `bounce_target` declared in this workflow.

      Preexisting Inputs
      Consume or update these existing local artifacts in place. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `safe/crates/glib/src/base/`
      - `safe/crates/glib/src/collections/`
      - `safe/crates/glib/src/mainloop/`
      - `safe/crates/glib/src/strings/`
      - `safe/crates/glib/src/threading/`
      - `safe/crates/glib/build.rs`
      - `safe/crates/glib/src/backend.rs`
      - `safe/crates/gthread/`
      - `safe/crates/gmodule/`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/build-glib-backend.py`
      - `safe/abi/link-compat/glib-core.json`
      - `safe/abi/layouts/glib.json`
      - `safe/abi/layouts/gthread.json`
      - `safe/abi/layouts/gmodule.json`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`
      - `safe/crates/glib/src/hash/api.rs`
      - `safe/crates/glib/src/gvariant/api.rs`
      - `safe/crates/glib/src/markup/api.rs`
      - `safe/crates/glib/src/charset/api.rs`
      - `safe/crates/glib/src/spawn/api.rs`
      - `safe/crates/glib/src/bytes/api.rs`
      - `safe/crates/glib/src/fileutils/api.rs`
      - `safe/crates/glib/src/keyfile/api.rs`
      - `original/glib/gbookmarkfile.c`
      - `original/glib/gcharset.c`
      - `original/glib/gconvert.c`
      - `original/glib/gfileutils.c`
      - `original/glib/ghash.c`
      - `original/glib/gkeyfile.c`
      - `original/glib/gmarkup.c`
      - `original/glib/gregex.c`
      - `original/glib/gscanner.c`
      - `original/glib/gspawn.c`
      - `original/glib/gunicode*.c`
      - `original/glib/guri.c`
      - `original/glib/gvariant*.c`
      - `relevant_cves.json`
      - `safe/tests/cve/*.c`
      - `safe/tests/manifests/glib-advanced.txt`
      - `safe/tests/manifests/fuzzing.txt`

      New Outputs
      Produce the phase outputs below, plus one git commit containing this phase's work before yielding. Do not leave required deliverables only in the worktree.
      - A pure-Rust `libglib-2.0.so.0` / `libglib-2.0.a` with no remaining dependency on replayed upstream GLib objects.
      - Updated CVE documentation and regression coverage.

      Artifact Handling Rules
      - Work from the current repository root and follow `AGENTS.md`.
      - Preserve unrelated user work; only edit files needed for this phase.
      - Consume or update existing artifacts instead of replacing them with fresh upstream fetches or newly rediscovered inventories.
      - If a listed artifact is stale, update it in place only when the phase source explicitly requires that change.
      - Commit the completed phase work to git before yielding to the verifier phases.

      Phase Source
      # Port Advanced GLib and Retire the GLib Backend Replay

      ## Phase Name
      Port advanced GLib and retire the GLib backend replay

      ## Implement Phase ID
      `impl-glib-advanced`

      ## Source Plan Context
      - Overall target remains GLib 2.80.0 compatibility on Ubuntu 24.04, with the existing hybrid bootstrap used only as an oracle and compatibility scaffold.
      - Relevant export count: `libglib-2.0.so.0` has 1872 symbols in `safe/abi/version-scripts/libglib.map`.
      - Relevant frozen manifests: `glib-advanced.txt` has 68 rows and `fuzzing.txt` has 22 rows.
      - Current Rust-owned CVE-sensitive GLib entry points already include hash, GVariant, markup, charset, spawn, bytes, fileutils, and keyfile APIs at the line ranges listed under Preexisting Inputs.
      - Current bootstrap state for this phase: `safe/crates/glib/build.rs` still links the replayed backend generated by `safe/tools/build-glib-backend.py`, which rewrites 96 frozen GLib compile commands and renames backend symbols. This phase must retire that backend from both dynamic and static library outputs.
      - `relevant_cves.json`, `safe/tests/cve/*`, `safe/docs/cve-matrix.md`, and the editable `safe/tests/upstream/glib/*` mirror are consumed in place and updated only when the phase changes their existing meaning.

      ## Preexisting Inputs
      - `safe/crates/glib/src/{base,collections,mainloop,strings,threading}/`
      - `safe/crates/glib/build.rs`
      - `safe/crates/glib/src/backend.rs`
      - `safe/crates/gthread/`
      - `safe/crates/gmodule/`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/build-glib-backend.py`
      - `safe/abi/link-compat/glib-core.json`
      - `safe/abi/layouts/{glib,gthread,gmodule}.json`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`
      - `safe/crates/glib/src/hash/api.rs:396-970`
      - `safe/crates/glib/src/gvariant/api.rs:229-280`
      - `safe/crates/glib/src/markup/api.rs:57-184`
      - `safe/crates/glib/src/charset/api.rs:82-144`
      - `safe/crates/glib/src/spawn/api.rs:140-316`
      - `safe/crates/glib/src/bytes/api.rs:11-20`
      - `safe/crates/glib/src/fileutils/api.rs:65-71`
      - `safe/crates/glib/src/keyfile/api.rs:18-36`
      - `original/glib/gbookmarkfile.c`
      - `original/glib/gcharset.c`
      - `original/glib/gconvert.c`
      - `original/glib/gfileutils.c`
      - `original/glib/ghash.c`
      - `original/glib/gkeyfile.c`
      - `original/glib/gmarkup.c`
      - `original/glib/gregex.c`
      - `original/glib/gscanner.c`
      - `original/glib/gspawn.c`
      - `original/glib/gunicode*.c`
      - `original/glib/guri.c`
      - `original/glib/gvariant*.c`
      - `relevant_cves.json`
      - `safe/tests/cve/*.c`
      - `safe/tests/manifests/glib-advanced.txt`
      - `safe/tests/manifests/fuzzing.txt`

      ## New Outputs
      - A pure-Rust `libglib-2.0.so.0` / `libglib-2.0.a` with no remaining dependency on replayed upstream GLib objects.
      - Updated CVE documentation and regression coverage.

      ## File Changes
      - `safe/crates/glib/src/bookmark/api.rs`
      - `safe/crates/glib/src/bytes/api.rs`
      - `safe/crates/glib/src/charset/api.rs`
      - `safe/crates/glib/src/fileutils/api.rs`
      - `safe/crates/glib/src/gvariant/api.rs`
      - `safe/crates/glib/src/hash/api.rs`
      - `safe/crates/glib/src/keyfile/api.rs`
      - `safe/crates/glib/src/markup/api.rs`
      - `safe/crates/glib/src/options/api.rs`
      - `safe/crates/glib/src/regex/api.rs`
      - `safe/crates/glib/src/scanner/api.rs`
      - `safe/crates/glib/src/spawn/api.rs`
      - `safe/crates/glib/src/unicode/api.rs`
      - `safe/crates/glib/src/uri/api.rs`
      - `safe/crates/glib/src/backend.rs`
      - `safe/crates/glib/src/bridge.rs`
      - `safe/crates/glib/build.rs`
      - `safe/tools/build-glib-backend.py`
      - `safe/abi/version-scripts/libglib.map`
      - `safe/abi/link-compat/glib-advanced.json`
      - `safe/tests/upstream/glib/markup-collect.c`
      - `safe/docs/cve-matrix.md`
      - `safe/crates/abi-support/src/ffi.rs` if shared public ABI primitives must be extended
      - `safe/crates/abi-support/src/bin/layout-probe.rs` if layout probing must cover newly Rust-owned public ABI types

      ## Implementation Details
      - Consume `relevant_cves.json`, `safe/tests/cve/*`, `safe/tests/manifests/glib-advanced.txt`, `safe/tests/manifests/fuzzing.txt`, and the existing editable upstream mirrors in place.
      - Expand the existing Rust interposition pattern until it owns the entire advanced GLib surface covered by `tests/manifests/glib-advanced.txt`.
      - Preserve the already-started security semantics.
      - `g_str_hash()` remains ABI-compatible, while Rust-owned hash tables stay collision-resilient.
      - `g_markup_parse_context_end_parse()` remains total over failed states and preserves the critical-message expectation already patched into `safe/tests/upstream/glib/markup-collect.c`.
      - Privileged charset handling clears `CHARSET`, `G_FILENAME_ENCODING`, and `G_BROKEN_FILENAMES`.
      - `g_byte_array_new_take()` rejects lengths above `G_MAXUINT`.
      - `g_variant_*` entry points validate layout and bound complexity before deep traversal.
      - Windows spawn wrappers reject overlong command lines.
      - Retire the replayed backend produced by `safe/tools/build-glib-backend.py`. That tool can remain only as a differential oracle if still useful for tests, but `safe/crates/glib/build.rs` must stop linking replayed upstream GLib objects into the final library.
      - Ensure the same retirement applies to the Cargo-produced static archive: `libglib-2.0.a` must not retain the replayed backend through `safe/crates/glib/build.rs`.
      - Extend `safe/crates/abi-support/src/ffi.rs` and `safe/crates/abi-support/src/bin/layout-probe.rs` only as advanced GLib public ABI types become Rust-owned, and keep any additions tied to updated layout baselines.
      - Update `safe/docs/cve-matrix.md` to match the final Rust-owned implementations and exact regression commands.
      - Critical file responsibilities owned by this phase:
        - `safe/tools/build-glib-backend.py` is the current GLib replay/rename bridge and must be reduced to an oracle or retired from shipped library builds as `libglib` becomes fully Rust-owned.
        - `safe/crates/glib/src/backend.rs` and `safe/crates/glib/src/bridge.rs` are temporary bootstrap delegation paths that must disappear from the final installed library or become test-only.
        - `safe/crates/glib/src/*/api.rs` files are the concrete GLib export implementation surface for `libglib-2.0`.
        - `safe/tools/run-cve-regressions.py` is the canonical GLib/GIO CVE regression harness; keep it synchronized with `relevant_cves.json`, `safe/tests/cve/*`, and the final implementation behavior.
        - `relevant_cves.json` is the canonical security scope, consumed in place by this phase and by later GIO/final verification.
        - `safe/tests/cve/*` are direct C probes for security-sensitive regressions.
        - `safe/docs/cve-matrix.md` is the final mapping from CVE scope to file-level mitigations and exact regression commands.

      ## Verification Phases
      ### `check-glib-advanced-link`
      - Phase ID: `check-glib-advanced-link`
      - Type: `check`
      - Fixed `bounce_target`: `impl-glib-advanced`
      - Purpose: Prove source- and link-compatibility for the non-core GLib exports.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-advanced --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-advanced/.stamp
      python3 tools/link-compat.py --phase glib-advanced --build-root build-glib-advanced --compile-original-objects --run
      ```

      ### `check-glib-advanced-tests`
      - Phase ID: `check-glib-advanced-tests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-glib-advanced`
      - Purpose: Replay the frozen advanced GLib manifest against the safe build.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-advanced --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-advanced/.stamp
      python3 tools/run-meson-manifest.py --build-root build-glib-advanced --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-glib-advanced/meson-info/intro-tests.json --manifest tests/manifests/glib-advanced.txt --print-errorlogs
      ```

      ### `check-glib-cves`
      - Phase ID: `check-glib-cves`
      - Type: `check`
      - Fixed `bounce_target`: `impl-glib-advanced`
      - Purpose: Re-run all GLib-specific CVE probes and fuzz-manifest rows against the safe build.
      - Commands:
      ```bash
      cd safe
      python3 tools/run-cve-regressions.py --phase glib --build-root build-glib-advanced --rebuild
      ```

      ## Success Criteria
      - `check-glib-advanced-link`, `check-glib-advanced-tests`, and `check-glib-cves` all pass.
      - `safe/crates/glib/src/bridge.rs` is gone or test-only.
      - `safe/crates/glib/src/backend.rs` is not required to satisfy final exported symbols.
      - `safe/crates/glib/build.rs` no longer archives or links the backend generated by `safe/tools/build-glib-backend.py`.

      ## Git Commit Requirement
      - The implementer must commit work to git before yielding.
      - This phase must produce at least one new git commit before yielding to its verification phases.
      - A verifier must treat an unchanged `HEAD` or a worktree-only deliverable as a failure.
  - id: check-glib-advanced-link
    type: check
    bounce_target: impl-glib-advanced
    prompt: |
      Reviewer role: You are the checker for phase `check-glib-advanced-link`, reviewing the work produced by `impl-glib-advanced`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `8`.
      - Phase ID: `check-glib-advanced-link`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-glib-advanced`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/glib/gbookmarkfile.c`
      - `original/glib/gcharset.c`
      - `original/glib/gconvert.c`
      - `original/glib/gfileutils.c`
      - `original/glib/ghash.c`
      - `original/glib/gkeyfile.c`
      - `original/glib/gmarkup.c`
      - `original/glib/gregex.c`
      - `original/glib/gscanner.c`
      - `original/glib/gspawn.c`
      - `original/glib/gunicode*.c`
      - `original/glib/guri.c`
      - `original/glib/gvariant*.c`
      - `safe/crates/glib/`
      - `safe/abi/link-compat/glib-advanced.json`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/link-compat.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-glib-advanced`.
      - Verify `impl-glib-advanced` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-glib-advanced` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-glib-advanced`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-advanced --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-advanced/.stamp
      python3 tools/link-compat.py --phase glib-advanced --build-root build-glib-advanced --compile-original-objects --run
      ```

      Phase Check Source
      ### `check-glib-advanced-link`
      - Phase ID: `check-glib-advanced-link`
      - Type: `check`
      - Fixed `bounce_target`: `impl-glib-advanced`
      - Purpose: Prove source- and link-compatibility for the non-core GLib exports.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-advanced --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-advanced/.stamp
      python3 tools/link-compat.py --phase glib-advanced --build-root build-glib-advanced --compile-original-objects --run
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-glib-advanced-tests
    type: check
    bounce_target: impl-glib-advanced
    prompt: |
      Reviewer role: You are the checker for phase `check-glib-advanced-tests`, reviewing the work produced by `impl-glib-advanced`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `9`.
      - Phase ID: `check-glib-advanced-tests`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-glib-advanced`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/glib/`
      - `safe/crates/glib/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/tests/manifests/glib-advanced.txt`
      - `safe/tests/upstream/*`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/run-meson-manifest.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-glib-advanced`.
      - Verify `impl-glib-advanced` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-glib-advanced` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-glib-advanced`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-advanced --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-advanced/.stamp
      python3 tools/run-meson-manifest.py --build-root build-glib-advanced --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-glib-advanced/meson-info/intro-tests.json --manifest tests/manifests/glib-advanced.txt --print-errorlogs
      ```

      Phase Check Source
      ### `check-glib-advanced-tests`
      - Phase ID: `check-glib-advanced-tests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-glib-advanced`
      - Purpose: Replay the frozen advanced GLib manifest against the safe build.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-glib-advanced --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-glib-advanced/.stamp
      python3 tools/run-meson-manifest.py --build-root build-glib-advanced --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-glib-advanced/meson-info/intro-tests.json --manifest tests/manifests/glib-advanced.txt --print-errorlogs
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-glib-cves
    type: check
    bounce_target: impl-glib-advanced
    prompt: |
      Reviewer role: You are the checker for phase `check-glib-cves`, reviewing the work produced by `impl-glib-advanced`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `10`.
      - Phase ID: `check-glib-cves`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-glib-advanced`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `relevant_cves.json`
      - `safe/crates/glib/`
      - `safe/tests/cve/*.c`
      - `safe/tests/manifests/fuzzing.txt`
      - `safe/tools/run-cve-regressions.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-glib-advanced`.
      - Verify `impl-glib-advanced` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-glib-advanced` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-glib-advanced`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/run-cve-regressions.py --phase glib --build-root build-glib-advanced --rebuild
      ```

      Phase Check Source
      ### `check-glib-cves`
      - Phase ID: `check-glib-cves`
      - Type: `check`
      - Fixed `bounce_target`: `impl-glib-advanced`
      - Purpose: Re-run all GLib-specific CVE probes and fuzz-manifest rows against the safe build.
      - Commands:
      ```bash
      cd safe
      python3 tools/run-cve-regressions.py --phase glib --build-root build-glib-advanced --rebuild
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl-gobject-rust
    type: implement
    bounce_target: null
    prompt: |
      Role: Implementer for phase `impl-gobject-rust`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `11`.
      - Phase ID: `impl-gobject-rust`.
      - Phase type: `implement`.
      - Fixed top-level `bounce_target`: `null`.
      - Verifier phases that immediately follow this implement block, in order: `check-gobject-link`, `check-gobject-tests`.
      - Do not invent, rename, reorder, merge, or split phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or any YAML-source indirection.
      - Do not introduce agent-selected `bounce_targets`; every verifier has the fixed `bounce_target` declared in this workflow.

      Preexisting Inputs
      Consume or update these existing local artifacts in place. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `safe/crates/glib/`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/run-cve-regressions.py`
      - `safe/docs/cve-matrix.md`
      - `safe/abi/link-compat/glib-advanced.json`
      - `original/gobject/*.c`
      - `original/gobject/*.h`
      - `original/gobject/tests/*`
      - `safe/crates/gobject/src/translated/original/gobject/*.rs`
      - `safe/crates/gobject/src/object/mod.rs`
      - `safe/crates/gobject/src/signal/mod.rs`
      - `safe/crates/gobject/src/type_system/mod.rs`
      - `safe/crates/gobject/src/value/mod.rs`
      - `safe/crates/gobject/build.rs`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`
      - `safe/tests/manifests/gobject.txt`

      New Outputs
      Produce the phase outputs below, plus one git commit containing this phase's work before yielding. Do not leave required deliverables only in the worktree.
      - A stable Rust-owned `libgobject-2.0.so.0` / `libgobject-2.0.a`.
      - Reduced warning surface and unified ABI types across translated modules.

      Artifact Handling Rules
      - Work from the current repository root and follow `AGENTS.md`.
      - Preserve unrelated user work; only edit files needed for this phase.
      - Consume or update existing artifacts instead of replacing them with fresh upstream fetches or newly rediscovered inventories.
      - If a listed artifact is stale, update it in place only when the phase source explicitly requires that change.
      - Commit the completed phase work to git before yielding to the verifier phases.

      Phase Source
      # Port GObject to a Stable Rust Runtime

      ## Phase Name
      Port GObject to a stable Rust runtime

      ## Implement Phase ID
      `impl-gobject-rust`

      ## Source Plan Context
      - Overall target remains GLib 2.80.0 compatibility on Ubuntu 24.04, building on the Rust-owned GLib core and advanced surfaces from earlier phases.
      - Relevant export count: `libgobject-2.0.so.0` has 478 symbols in `safe/abi/version-scripts/libgobject.map`.
      - Relevant frozen manifest: `safe/tests/manifests/gobject.txt` has 62 rows.
      - Current bootstrap state for this phase: `safe/crates/gobject` is mostly translated Rust under `safe/crates/gobject/src/translated/original/gobject/*.rs`; it compiles, but `cargo check --workspace` emits many warnings and the repository-wide unsafe audit still fails.
      - The editable upstream mirror already contains a safe-specific `closure-refcount` timeout adjustment in `safe/tests/upstream/gobject/meson.build`; preserve it in place.

      ## Preexisting Inputs
      - `safe/crates/glib/`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/run-cve-regressions.py`
      - `safe/docs/cve-matrix.md`
      - `safe/abi/link-compat/glib-advanced.json`
      - `original/gobject/*.c`, `original/gobject/*.h`, and `original/gobject/tests/*`
      - `safe/crates/gobject/src/translated/original/gobject/*.rs`
      - `safe/crates/gobject/src/object/mod.rs`
      - `safe/crates/gobject/src/signal/mod.rs`
      - `safe/crates/gobject/src/type_system/mod.rs`
      - `safe/crates/gobject/src/value/mod.rs`
      - `safe/crates/gobject/build.rs`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`
      - `safe/tests/manifests/gobject.txt`

      ## New Outputs
      - A stable Rust-owned `libgobject-2.0.so.0` / `libgobject-2.0.a`.
      - Reduced warning surface and unified ABI types across translated modules.

      ## File Changes
      - `safe/crates/gobject/src/lib.rs`
      - `safe/crates/gobject/src/object/mod.rs`
      - `safe/crates/gobject/src/signal/mod.rs`
      - `safe/crates/gobject/src/tools/mod.rs`
      - `safe/crates/gobject/src/type_system/mod.rs`
      - `safe/crates/gobject/src/value/mod.rs`
      - `safe/crates/gobject/src/translated/compat.rs`
      - `safe/crates/gobject/src/translated/original/gobject/*.rs`
      - `safe/crates/gobject/build.rs`
      - `safe/abi/version-scripts/libgobject.map`
      - `safe/abi/layout-manifests/gobject.json`
      - `safe/abi/layouts/gobject.json`
      - `safe/tests/upstream/gobject/meson.build`
      - `safe/crates/abi-support/src/ffi.rs` if shared public ABI primitives must be extended
      - `safe/crates/abi-support/src/bin/layout-probe.rs` if layout probing must cover newly Rust-owned public ABI types

      ## Implementation Details
      - Consume `original/gobject/*`, `safe/tests/manifests/gobject.txt`, and the editable `safe/tests/upstream/gobject/` mirror in place.
      - Keep the translated-Rust strategy as the starting point, but turn it into a coherent runtime instead of a direct file-for-file mechanical port.
      - Replace duplicated opaque typedef islands and incompatible forward declarations across translated files with shared ABI structs from `safe/crates/gobject/src/object/mod.rs`, `safe/crates/gobject/src/signal/mod.rs`, `safe/crates/gobject/src/type_system/mod.rs`, and `safe/crates/gobject/src/value/mod.rs`.
      - Extend `safe/crates/abi-support/src/ffi.rs` and `safe/crates/abi-support/src/bin/layout-probe.rs` only as GObject public ABI types become Rust-owned, and keep the shared definitions synchronized with `safe/abi/layout-manifests/gobject.json` and `safe/abi/layouts/gobject.json`.
      - Preserve the behaviors that `safe/tools/run-meson-manifest.py` explicitly treats as required coverage for `gobject.txt`: closure ownership and refcount, signal callback ordering, and threaded and performance test stability.
      - Keep `glib-genmarshal`, `glib-mkenums`, and `gobject-query` build products working because package and build tests consume them.
      - Move unsafe code toward explicit ABI/FFI boundaries only; document each remaining boundary in source comments so the final unsafe audit can become strict without breaking required FFI.
      - Critical file responsibilities owned by this phase:
        - `safe/crates/gobject/src/translated/original/gobject/*.rs` is the current translated GObject runtime; clean or replace this code in place.
        - `safe/crates/gobject/src/{object,signal,type_system,value}/mod.rs` define the shared runtime and ABI structures that translated files must converge on.
        - `safe/abi/version-scripts/libgobject.map`, `safe/abi/layout-manifests/gobject.json`, and `safe/abi/layouts/gobject.json` are the symbol and public-layout contracts for `libgobject-2.0`.
        - `safe/tests/upstream/gobject/meson.build` remains an editable mirror file and keeps the safe-specific `closure-refcount` timeout adjustment.

      ## Verification Phases
      ### `check-gobject-link`
      - Phase ID: `check-gobject-link`
      - Type: `check`
      - Fixed `bounce_target`: `impl-gobject-rust`
      - Purpose: Prove link-compatibility for `libgobject-2.0.so.0`.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gobject --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gobject/.stamp
      python3 tools/link-compat.py --phase gobject --build-root build-gobject --compile-original-objects --run
      ```

      ### `check-gobject-tests`
      - Phase ID: `check-gobject-tests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-gobject-rust`
      - Purpose: Replay the frozen GObject manifest, including the required closure and signal regressions enforced by `run-meson-manifest.py`.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gobject --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gobject/.stamp
      python3 tools/run-meson-manifest.py --build-root build-gobject --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-gobject/meson-info/intro-tests.json --manifest tests/manifests/gobject.txt --print-errorlogs
      cargo check --workspace
      ```

      ## Success Criteria
      - `check-gobject-link` and `check-gobject-tests` both pass.
      - The large type-redeclaration warning class from the translated modules is substantially reduced or eliminated.

      ## Git Commit Requirement
      - The implementer must commit work to git before yielding.
      - This phase must produce at least one new git commit before yielding to its verification phases.
      - A verifier must treat an unchanged `HEAD` or a worktree-only deliverable as a failure.
  - id: check-gobject-link
    type: check
    bounce_target: impl-gobject-rust
    prompt: |
      Reviewer role: You are the checker for phase `check-gobject-link`, reviewing the work produced by `impl-gobject-rust`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `12`.
      - Phase ID: `check-gobject-link`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-gobject-rust`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/gobject/*.c`
      - `original/gobject/*.h`
      - `original/gobject/tests/*`
      - `safe/crates/gobject/`
      - `safe/abi/link-compat/gobject.json`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/link-compat.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-gobject-rust`.
      - Verify `impl-gobject-rust` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-gobject-rust` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-gobject-rust`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gobject --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gobject/.stamp
      python3 tools/link-compat.py --phase gobject --build-root build-gobject --compile-original-objects --run
      ```

      Phase Check Source
      ### `check-gobject-link`
      - Phase ID: `check-gobject-link`
      - Type: `check`
      - Fixed `bounce_target`: `impl-gobject-rust`
      - Purpose: Prove link-compatibility for `libgobject-2.0.so.0`.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gobject --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gobject/.stamp
      python3 tools/link-compat.py --phase gobject --build-root build-gobject --compile-original-objects --run
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-gobject-tests
    type: check
    bounce_target: impl-gobject-rust
    prompt: |
      Reviewer role: You are the checker for phase `check-gobject-tests`, reviewing the work produced by `impl-gobject-rust`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `13`.
      - Phase ID: `check-gobject-tests`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-gobject-rust`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/gobject/*.c`
      - `original/gobject/*.h`
      - `original/gobject/tests/*`
      - `safe/crates/gobject/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/tests/manifests/gobject.txt`
      - `safe/tests/upstream/gobject/`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/run-meson-manifest.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-gobject-rust`.
      - Verify `impl-gobject-rust` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-gobject-rust` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-gobject-rust`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gobject --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gobject/.stamp
      python3 tools/run-meson-manifest.py --build-root build-gobject --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-gobject/meson-info/intro-tests.json --manifest tests/manifests/gobject.txt --print-errorlogs
      cargo check --workspace
      ```

      Phase Check Source
      ### `check-gobject-tests`
      - Phase ID: `check-gobject-tests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-gobject-rust`
      - Purpose: Replay the frozen GObject manifest, including the required closure and signal regressions enforced by `run-meson-manifest.py`.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gobject --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gobject/.stamp
      python3 tools/run-meson-manifest.py --build-root build-gobject --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-gobject/meson-info/intro-tests.json --manifest tests/manifests/gobject.txt --print-errorlogs
      cargo check --workspace
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl-gio-rust
    type: implement
    bounce_target: null
    prompt: |
      Role: Implementer for phase `impl-gio-rust`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `14`.
      - Phase ID: `impl-gio-rust`.
      - Phase type: `implement`.
      - Fixed top-level `bounce_target`: `null`.
      - Verifier phases that immediately follow this implement block, in order: `check-gio-link`, `check-gio-tests`, `check-gio-cves`.
      - Do not invent, rename, reorder, merge, or split phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or any YAML-source indirection.
      - Do not introduce agent-selected `bounce_targets`; every verifier has the fixed `bounce_target` declared in this workflow.

      Preexisting Inputs
      Consume or update these existing local artifacts in place. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `safe/crates/gobject/`
      - `safe/abi/layout-manifests/gobject.json`
      - `safe/abi/layouts/gobject.json`
      - `safe/abi/version-scripts/libgobject.map`
      - `safe/tests/upstream/gobject/meson.build`
      - `original/gio/*.c`
      - `original/gio/*.h`
      - `original/gio/tests/*`
      - `safe/crates/gio/build.rs`
      - `safe/crates/gio/src/lib.rs`
      - `safe/crates/gio/src/runtime.rs`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`
      - `safe/abi/link-compat/gio.json`
      - `safe/abi/installed-files.json`
      - `safe/debian/libglib2.0-0t64.install`
      - `safe/debian/libglib2.0-bin.install`
      - `safe/debian/libglib2.0-dev-bin.install`
      - `safe/tools/stage-package-tree.py`
      - `safe/tests/manifests/gio.txt`
      - `safe/tests/cve/keyfile-settings-backend.c`
      - `relevant_cves.json`

      New Outputs
      Produce the phase outputs below, plus one git commit containing this phase's work before yielding. Do not leave required deliverables only in the worktree.
      - A Rust-owned `libgio-2.0.so.0` / `libgio-2.0.a`.
      - Rust-owned build-root and package-staging GIO helper tools, libexec helpers, and pkg-config variables consistent with upstream.

      Artifact Handling Rules
      - Work from the current repository root and follow `AGENTS.md`.
      - Preserve unrelated user work; only edit files needed for this phase.
      - Consume or update existing artifacts instead of replacing them with fresh upstream fetches or newly rediscovered inventories.
      - If a listed artifact is stale, update it in place only when the phase source explicitly requires that change.
      - Commit the completed phase work to git before yielding to the verifier phases.

      Phase Source
      # Port GIO to Rust

      ## Phase Name
      Port GIO to Rust

      ## Implement Phase ID
      `impl-gio-rust`

      ## Source Plan Context
      - Overall target remains GLib 2.80.0 compatibility on Ubuntu 24.04, now extending the already Rust-owned GLib and GObject layers to the GIO surface.
      - Relevant export count: `libgio-2.0.so.0` has 2107 symbols in `safe/abi/version-scripts/libgio.map`.
      - Relevant frozen manifest: `safe/tests/manifests/gio.txt` has 133 rows.
      - Current bootstrap state for this phase: `safe/crates/gio/build.rs` still injects upstream GIO objects and archives from `safe/vendor/build-check/gio`, and `safe/tools/build-abi-shell.py` still has a vendored static-archive fallback for `libgio-2.0.a`.
      - GIO helper executables, libexec helpers, installed file manifests, Debian install lists, and pkg-config variables are existing package artifacts to preserve and replace from `safe/` outputs, not rediscover from upstream.
      - GIO security scope is already captured in `relevant_cves.json`, `safe/tests/cve/keyfile-settings-backend.c`, and `safe/tools/run-cve-regressions.py`; consume these in place.

      ## Preexisting Inputs
      - `safe/crates/gobject/`
      - `safe/abi/layout-manifests/gobject.json`
      - `safe/abi/layouts/gobject.json`
      - `safe/abi/version-scripts/libgobject.map`
      - `safe/tests/upstream/gobject/meson.build`
      - `original/gio/*.c`, `original/gio/*.h`, and `original/gio/tests/*`
      - `safe/crates/gio/build.rs`
      - `safe/crates/gio/src/lib.rs`
      - `safe/crates/gio/src/runtime.rs`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`
      - `safe/abi/link-compat/gio.json`
      - `safe/abi/installed-files.json`
      - `safe/debian/libglib2.0-0t64.install`
      - `safe/debian/libglib2.0-bin.install`
      - `safe/debian/libglib2.0-dev-bin.install`
      - `safe/tools/stage-package-tree.py`
      - `safe/tests/manifests/gio.txt`
      - `safe/tests/cve/keyfile-settings-backend.c`
      - `relevant_cves.json`

      ## New Outputs
      - A Rust-owned `libgio-2.0.so.0` / `libgio-2.0.a`.
      - Rust-owned build-root and package-staging GIO helper tools, libexec helpers, and pkg-config variables consistent with upstream.

      ## File Changes
      - `safe/crates/gio/build.rs`
      - `safe/crates/gio/src/lib.rs`
      - `safe/crates/gio/src/runtime.rs`
      - `safe/crates/gio/src/exports.rs`
      - New Rust submodules under `safe/crates/gio/src/` for actions and application, file and local-file, streams and memory streams, sockets and networking, proxy resolution, resources and settings, GDBus core, and tools and command-line helpers
      - `safe/crates/abi-support/src/ffi.rs` if shared public ABI primitives must be extended
      - `safe/crates/abi-support/src/bin/layout-probe.rs` if layout probing must cover newly Rust-owned public ABI types
      - `safe/tools/build-abi-shell.py`
      - `safe/abi/version-scripts/libgio.map`
      - `safe/abi/link-compat/gio.json`
      - `safe/tests/upstream/gio/*` where local mirror adjustments are needed

      ## Implementation Details
      - Consume `original/gio/*`, `safe/tests/manifests/gio.txt`, `safe/tests/upstream/gio/*`, `relevant_cves.json`, and the recorded package-install artifacts in place.
      - Replace the current direct object/archive injection in `safe/crates/gio/build.rs` with Rust modules. If an intermediate compatibility backend is necessary, add it explicitly and then remove it before the phase ends; do not leave direct upstream object linkage in the final library.
      - Retire the vendored static-archive fallback for `libgio-2.0.a` in `safe/tools/build-abi-shell.py`; static-link consumers must exercise the Rust implementation too.
      - Port the file, stream, socket, application, settings, proxy, and D-Bus stacks in clusters that match the upstream tests and dependent runtime surface.
      - Extend `safe/crates/abi-support/src/ffi.rs` and `safe/crates/abi-support/src/bin/layout-probe.rs` only as GIO public ABI types become Rust-owned, and keep any additions tied to updated GIO layout contracts.
      - Treat `safe/abi/installed-files.json`, `safe/debian/libglib2.0-0t64.install`, `safe/debian/libglib2.0-bin.install`, and `safe/debian/libglib2.0-dev-bin.install` as the authoritative shipped GIO helper surface for later packaging. Phase completion requires the matching build-root artifacts to exist at the same logical paths without fallback to `safe/vendor/build-check`.
      - Preserve the GIO-specific security behaviors already captured in `relevant_cves.json` and `safe/tools/run-cve-regressions.py`.
      - Secure `g_file_copy()` and replace-destination behavior.
      - Private-permission `GKeyfileSettingsBackend`.
      - No direct-connect fallback once proxy routing is selected.
      - Trusted-sender validation for `g_dbus_connection_signal_subscribe()`.
      - Keep the shipped helper executables, libexec helpers, and pkg-config variables working: `gapplication`, `gio`, `gdbus`, `gsettings`, `gresource`, `gio-launch-desktop`, `glib-compile-schemas`, `glib-compile-resources`, `gio-querymodules`, and `gdbus-codegen` plus its installed `gdbus-2.0/codegen/*.py` support modules.
      - Do not leave the shipped helper executable or libexec-helper paths copied verbatim from `safe/vendor/build-check`; by the end of the phase the build-root paths later consumed by packaging must be rebuilt or replaced from `safe/` outputs.
      - Critical file responsibilities owned by this phase:
        - `safe/crates/gio/build.rs`, `safe/crates/gio/src/lib.rs`, and `safe/crates/gio/src/runtime.rs` are the current GIO bootstrap surface that still depends on vendored original objects.
        - New modules under `safe/crates/gio/src/` must turn GIO into a real Rust crate while preserving the 2107-symbol public surface recorded in `safe/abi/version-scripts/libgio.map`.
        - `safe/abi/link-compat/gio.json` and `safe/tests/manifests/gio.txt` are updated only in place as GIO coverage becomes Rust-owned.
        - `safe/abi/installed-files.json` and the `safe/debian/libglib2.0-*.install` files are authoritative for the shipped GIO helper and libexec surface consumed by packaging.

      ## Verification Phases
      ### `check-gio-link`
      - Phase ID: `check-gio-link`
      - Type: `check`
      - Fixed `bounce_target`: `impl-gio-rust`
      - Purpose: Prove that the full `libgio-2.0.so.0` public surface still links and runs from original objects.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gio --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gio/.stamp
      python3 tools/link-compat.py --phase gio --build-root build-gio --compile-original-objects --run
      ```

      ### `check-gio-tests`
      - Phase ID: `check-gio-tests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-gio-rust`
      - Purpose: Replay the frozen GIO manifest against the safe build.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gio --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gio/.stamp
      python3 tools/run-meson-manifest.py --build-root build-gio --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-gio/meson-info/intro-tests.json --manifest tests/manifests/gio.txt --print-errorlogs
      ```

      ### `check-gio-cves`
      - Phase ID: `check-gio-cves`
      - Type: `check`
      - Fixed `bounce_target`: `impl-gio-rust`
      - Purpose: Re-run the GIO CVE probes and targeted manifest rows for file copy, settings backend, proxy routing, and sender validation.
      - Commands:
      ```bash
      cd safe
      python3 tools/run-cve-regressions.py --phase gio --build-root build-gio --rebuild
      ```

      ## Success Criteria
      - `check-gio-link`, `check-gio-tests`, and `check-gio-cves` all pass.
      - `safe/crates/gio/build.rs` no longer injects `safe/vendor/build-check/gio/*.o` or the vendored `xdgmime` / `inotify` archives into the final cdylib.
      - `safe/tools/build-abi-shell.py` no longer assembles `libgio-2.0.a` from vendored objects or leaves vendored GIO helper executables/libexec helpers at the shipped `gapplication`, `gio`, `gdbus`, `gsettings`, `gresource`, `gio-launch-desktop`, `glib-compile-schemas`, `glib-compile-resources`, `gio-querymodules`, or `gdbus-codegen` paths.

      ## Git Commit Requirement
      - The implementer must commit work to git before yielding.
      - This phase must produce at least one new git commit before yielding to its verification phases.
      - A verifier must treat an unchanged `HEAD` or a worktree-only deliverable as a failure.
  - id: check-gio-link
    type: check
    bounce_target: impl-gio-rust
    prompt: |
      Reviewer role: You are the checker for phase `check-gio-link`, reviewing the work produced by `impl-gio-rust`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `15`.
      - Phase ID: `check-gio-link`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-gio-rust`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/gio/*.c`
      - `original/gio/*.h`
      - `original/gio/tests/*`
      - `safe/crates/gio/`
      - `safe/abi/link-compat/gio.json`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/link-compat.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-gio-rust`.
      - Verify `impl-gio-rust` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-gio-rust` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-gio-rust`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gio --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gio/.stamp
      python3 tools/link-compat.py --phase gio --build-root build-gio --compile-original-objects --run
      ```

      Phase Check Source
      ### `check-gio-link`
      - Phase ID: `check-gio-link`
      - Type: `check`
      - Fixed `bounce_target`: `impl-gio-rust`
      - Purpose: Prove that the full `libgio-2.0.so.0` public surface still links and runs from original objects.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gio --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gio/.stamp
      python3 tools/link-compat.py --phase gio --build-root build-gio --compile-original-objects --run
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-gio-tests
    type: check
    bounce_target: impl-gio-rust
    prompt: |
      Reviewer role: You are the checker for phase `check-gio-tests`, reviewing the work produced by `impl-gio-rust`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `16`.
      - Phase ID: `check-gio-tests`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-gio-rust`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/gio/*.c`
      - `original/gio/*.h`
      - `original/gio/tests/*`
      - `safe/crates/gio/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/tests/manifests/gio.txt`
      - `safe/tests/upstream/gio/*`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/run-meson-manifest.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-gio-rust`.
      - Verify `impl-gio-rust` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-gio-rust` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-gio-rust`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gio --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gio/.stamp
      python3 tools/run-meson-manifest.py --build-root build-gio --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-gio/meson-info/intro-tests.json --manifest tests/manifests/gio.txt --print-errorlogs
      ```

      Phase Check Source
      ### `check-gio-tests`
      - Phase ID: `check-gio-tests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-gio-rust`
      - Purpose: Replay the frozen GIO manifest against the safe build.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-gio --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-gio/.stamp
      python3 tools/run-meson-manifest.py --build-root build-gio --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-gio/meson-info/intro-tests.json --manifest tests/manifests/gio.txt --print-errorlogs
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-gio-cves
    type: check
    bounce_target: impl-gio-rust
    prompt: |
      Reviewer role: You are the checker for phase `check-gio-cves`, reviewing the work produced by `impl-gio-rust`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `17`.
      - Phase ID: `check-gio-cves`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-gio-rust`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `relevant_cves.json`
      - `safe/crates/gio/`
      - `safe/tests/cve/keyfile-settings-backend.c`
      - `safe/tools/run-cve-regressions.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-gio-rust`.
      - Verify `impl-gio-rust` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-gio-rust` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-gio-rust`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/run-cve-regressions.py --phase gio --build-root build-gio --rebuild
      ```

      Phase Check Source
      ### `check-gio-cves`
      - Phase ID: `check-gio-cves`
      - Type: `check`
      - Fixed `bounce_target`: `impl-gio-rust`
      - Purpose: Re-run the GIO CVE probes and targeted manifest rows for file copy, settings backend, proxy routing, and sender validation.
      - Commands:
      ```bash
      cd safe
      python3 tools/run-cve-regressions.py --phase gio --build-root build-gio --rebuild
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl-girepository-rust
    type: implement
    bounce_target: null
    prompt: |
      Role: Implementer for phase `impl-girepository-rust`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `18`.
      - Phase ID: `impl-girepository-rust`.
      - Phase type: `implement`.
      - Fixed top-level `bounce_target`: `null`.
      - Verifier phases that immediately follow this implement block, in order: `check-girepository-link`, `check-girepository-tests`.
      - Do not invent, rename, reorder, merge, or split phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or any YAML-source indirection.
      - Do not introduce agent-selected `bounce_targets`; every verifier has the fixed `bounce_target` declared in this workflow.

      Preexisting Inputs
      Consume or update these existing local artifacts in place. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `safe/crates/gio/`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/stage-package-tree.py`
      - `safe/abi/link-compat/gio.json`
      - `safe/abi/installed-files.json`
      - `safe/debian/libglib2.0-0t64.install`
      - `safe/debian/libglib2.0-bin.install`
      - `safe/debian/libglib2.0-dev-bin.install`
      - `original/girepository/*.c`
      - `original/girepository/*.h`
      - `original/girepository/tests/*`
      - `safe/crates/girepository/src/exports.rs`
      - `safe/crates/girepository/src/runtime.rs`
      - `safe/crates/girepository/src/repository/mod.rs`
      - `safe/crates/girepository/src/parser/mod.rs`
      - `safe/crates/girepository/src/invoke/mod.rs`
      - `safe/crates/girepository/src/tools/mod.rs`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`
      - `safe/tests/manifests/girepository.txt`
      - `safe/tests/package/girepository-compile-only.sh`
      - `safe/tests/package/girepository-installed.sh`

      New Outputs
      Produce the phase outputs below, plus one git commit containing this phase's work before yielding. Do not leave required deliverables only in the worktree.
      - A real Rust `libgirepository-2.0.so.0` / `libgirepository-2.0.a`.
      - Working build-root `gi-compile-repository`, `gi-decompile-typelib`, and `gi-inspect-typelib`.
      - GIR and typelib artifacts that remain consistent with the compile-only and installed-package consumers, with installed-package execution of those scripts completed in phase 7.

      Artifact Handling Rules
      - Work from the current repository root and follow `AGENTS.md`.
      - Preserve unrelated user work; only edit files needed for this phase.
      - Consume or update existing artifacts instead of replacing them with fresh upstream fetches or newly rediscovered inventories.
      - If a listed artifact is stale, update it in place only when the phase source explicitly requires that change.
      - Commit the completed phase work to git before yielding to the verifier phases.

      Phase Source
      # Port GIRepository and the `gi-*` Tools

      ## Phase Name
      Port GIRepository and the `gi-*` tools

      ## Implement Phase ID
      `impl-girepository-rust`

      ## Source Plan Context
      - Overall target remains GLib 2.80.0 compatibility on Ubuntu 24.04, with GIRepository becoming a real Rust library and toolchain after GIO is Rust-owned.
      - Relevant export count: `libgirepository-2.0.so.0` has 231 symbols in `safe/abi/version-scripts/libgirepository.map`.
      - Relevant frozen manifest: `safe/tests/manifests/girepository.txt` has 11 rows.
      - Current bootstrap state for this phase: `safe/crates/girepository/src/exports.rs` exports 229 placeholder symbols plus 2 real exports, and `safe/crates/girepository/src/runtime.rs` contains only minimal real runtime functions.
      - `safe/tests/package/girepository-compile-only.sh` and `safe/tests/package/girepository-installed.sh` are existing package-facing acceptance specs. This phase satisfies their interface assumptions, while phase 7 executes them after installing safe packages.

      ## Preexisting Inputs
      - `safe/crates/gio/`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/stage-package-tree.py`
      - `safe/abi/link-compat/gio.json`
      - `safe/abi/installed-files.json`
      - `safe/debian/libglib2.0-0t64.install`
      - `safe/debian/libglib2.0-bin.install`
      - `safe/debian/libglib2.0-dev-bin.install`
      - `original/girepository/*.c`, `original/girepository/*.h`, and `original/girepository/tests/*`
      - `safe/crates/girepository/src/exports.rs`
      - `safe/crates/girepository/src/runtime.rs`
      - `safe/crates/girepository/src/repository/mod.rs`
      - `safe/crates/girepository/src/parser/mod.rs`
      - `safe/crates/girepository/src/invoke/mod.rs`
      - `safe/crates/girepository/src/tools/mod.rs`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`
      - `safe/tests/manifests/girepository.txt`
      - `safe/tests/package/girepository-compile-only.sh`
      - `safe/tests/package/girepository-installed.sh`

      ## New Outputs
      - A real Rust `libgirepository-2.0.so.0` / `libgirepository-2.0.a`.
      - Working build-root `gi-compile-repository`, `gi-decompile-typelib`, and `gi-inspect-typelib`.
      - GIR and typelib artifacts that remain consistent with the compile-only and installed-package consumers, with installed-package execution of those scripts completed in phase 7.

      ## File Changes
      - `safe/crates/girepository/build.rs`
      - `safe/crates/girepository/src/lib.rs`
      - `safe/crates/girepository/src/exports.rs`
      - `safe/crates/girepository/src/runtime.rs`
      - `safe/crates/girepository/src/repository/mod.rs`
      - `safe/crates/girepository/src/parser/mod.rs`
      - `safe/crates/girepository/src/invoke/mod.rs`
      - `safe/crates/girepository/src/tools/mod.rs`
      - New Rust submodules under `safe/crates/girepository/src/` for typelib parsing, repository queries, and invoke support
      - `safe/crates/abi-support/src/ffi.rs` if shared public ABI primitives must be extended
      - `safe/crates/abi-support/src/bin/layout-probe.rs` if layout probing must cover newly Rust-owned public ABI types
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/stage-package-tree.py`
      - `safe/abi/version-scripts/libgirepository.map`
      - `safe/abi/link-compat/girepository.json`

      ## Implementation Details
      - Consume `original/girepository/*`, `safe/tests/manifests/girepository.txt`, and the package-facing GIRepository test scripts in place.
      - Replace the placeholder export wall in `safe/crates/girepository/src/exports.rs` with real Rust functions. The current state is symbol-complete but runtime-incomplete; the final phase output must remove `PLACEHOLDER_*` entirely.
      - Replace the vendored static-archive fallback for `libgirepository-2.0.a` in `safe/tools/build-abi-shell.py`; the static archive shipped to developers must exercise the safe implementation.
      - Implement the repository loader, typelib parser/validator, callable info queries, type-info helpers, and invoke routines used by `tests/manifests/girepository.txt`, the package consumer in `safe/tests/package/girepository-consumer.c`, and the installed tool smoke tests that phase 7 runs through `safe/tests/package/girepository-installed.sh`.
      - Extend `safe/crates/abi-support/src/ffi.rs` and `safe/crates/abi-support/src/bin/layout-probe.rs` only as GIRepository public ABI types become Rust-owned, and keep those definitions synchronized with the GIRepository layout and symbol contracts.
      - Preserve the installed artifact layout already described in `safe/crates/girepository/src/repository/mod.rs`, `safe/crates/girepository/src/parser/mod.rs`, and `safe/crates/girepository/src/tools/mod.rs`.
      - Keep the Debian cross-wrapper contract for `gi-*` tools intact, because `safe/debian/rules` already synthesizes cross-prefixed wrappers for them.
      - Do not leave the build-root `gi-compile-repository`, `gi-decompile-typelib`, or `gi-inspect-typelib` paths copied from `safe/vendor/build-check`; phase completion requires those shipped tool paths to come from `safe/` outputs.
      - Treat `safe/tests/package/girepository-compile-only.sh` and `safe/tests/package/girepository-installed.sh` as the package-facing acceptance specs. Phase 6 should satisfy their interface assumptions, but phase 7 is where those exact scripts are executed after package installation.
      - Critical file responsibilities owned by this phase:
        - `safe/crates/girepository/src/exports.rs` is the current placeholder export wall; replace every `PLACEHOLDER_` export with real implementation.
        - `safe/crates/girepository/src/{runtime.rs,repository/mod.rs,parser/mod.rs,invoke/mod.rs,tools/mod.rs}` is the current minimal metadata/runtime surface that must become a real library and toolchain.
        - `safe/abi/version-scripts/libgirepository.map` and `safe/abi/link-compat/girepository.json` are the symbol and link/run contracts for `libgirepository-2.0`.
        - `safe/tests/package/girepository-compile-only.sh` and `safe/tests/package/girepository-installed.sh` are package-level acceptance specs that must remain compatible for phase 7 installed-package execution.

      ## Verification Phases
      ### `check-girepository-link`
      - Phase ID: `check-girepository-link`
      - Type: `check`
      - Fixed `bounce_target`: `impl-girepository-rust`
      - Purpose: Prove that original object files compiled against the installed GIRepository headers still link and run.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-girepository --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-girepository/.stamp
      python3 tools/link-compat.py --phase girepository --build-root build-girepository --compile-original-objects --run
      ```

      ### `check-girepository-tests`
      - Phase ID: `check-girepository-tests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-girepository-rust`
      - Purpose: Replay the frozen GIRepository manifest and verify the build-root library surface; installed-package tool execution is deferred to phase 7.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-girepository --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-girepository/.stamp
      python3 tools/run-meson-manifest.py --build-root build-girepository --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-girepository/meson-info/intro-tests.json --manifest tests/manifests/girepository.txt --print-errorlogs
      cargo check --workspace
      ! rg -n "PLACEHOLDER_" crates/girepository/src/exports.rs
      ```

      ## Success Criteria
      - `check-girepository-link` and `check-girepository-tests` both pass.
      - There are no `PLACEHOLDER_` exports left in `safe/crates/girepository/src/exports.rs`.
      - `safe/tools/build-abi-shell.py` no longer sources `libgirepository-2.0.a` or the shipped `gi-*` tool paths from `safe/vendor/build-check`.

      ## Git Commit Requirement
      - The implementer must commit work to git before yielding.
      - This phase must produce at least one new git commit before yielding to its verification phases.
      - A verifier must treat an unchanged `HEAD` or a worktree-only deliverable as a failure.
  - id: check-girepository-link
    type: check
    bounce_target: impl-girepository-rust
    prompt: |
      Reviewer role: You are the checker for phase `check-girepository-link`, reviewing the work produced by `impl-girepository-rust`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `19`.
      - Phase ID: `check-girepository-link`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-girepository-rust`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/girepository/*.c`
      - `original/girepository/*.h`
      - `original/girepository/tests/*`
      - `safe/crates/girepository/`
      - `safe/abi/link-compat/girepository.json`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/link-compat.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-girepository-rust`.
      - Verify `impl-girepository-rust` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-girepository-rust` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-girepository-rust`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-girepository --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-girepository/.stamp
      python3 tools/link-compat.py --phase girepository --build-root build-girepository --compile-original-objects --run
      ```

      Phase Check Source
      ### `check-girepository-link`
      - Phase ID: `check-girepository-link`
      - Type: `check`
      - Fixed `bounce_target`: `impl-girepository-rust`
      - Purpose: Prove that original object files compiled against the installed GIRepository headers still link and run.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-girepository --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-girepository/.stamp
      python3 tools/link-compat.py --phase girepository --build-root build-girepository --compile-original-objects --run
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-girepository-tests
    type: check
    bounce_target: impl-girepository-rust
    prompt: |
      Reviewer role: You are the checker for phase `check-girepository-tests`, reviewing the work produced by `impl-girepository-rust`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `20`.
      - Phase ID: `check-girepository-tests`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-girepository-rust`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `original/girepository/*.c`
      - `original/girepository/*.h`
      - `original/girepository/tests/*`
      - `safe/crates/girepository/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/tests/manifests/girepository.txt`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/run-meson-manifest.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-girepository-rust`.
      - Verify `impl-girepository-rust` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-girepository-rust` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-girepository-rust`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-girepository --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-girepository/.stamp
      python3 tools/run-meson-manifest.py --build-root build-girepository --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-girepository/meson-info/intro-tests.json --manifest tests/manifests/girepository.txt --print-errorlogs
      cargo check --workspace
      ! rg -n "PLACEHOLDER_" crates/girepository/src/exports.rs
      ```

      Phase Check Source
      ### `check-girepository-tests`
      - Phase ID: `check-girepository-tests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-girepository-rust`
      - Purpose: Replay the frozen GIRepository manifest and verify the build-root library surface; installed-package tool execution is deferred to phase 7.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-girepository --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-girepository/.stamp
      python3 tools/run-meson-manifest.py --build-root build-girepository --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-girepository/meson-info/intro-tests.json --manifest tests/manifests/girepository.txt --print-errorlogs
      cargo check --workspace
      ! rg -n "PLACEHOLDER_" crates/girepository/src/exports.rs
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl-package-integration
    type: implement
    bounce_target: null
    prompt: |
      Role: Implementer for phase `impl-package-integration`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `21`.
      - Phase ID: `impl-package-integration`.
      - Phase type: `implement`.
      - Fixed top-level `bounce_target`: `null`.
      - Verifier phases that immediately follow this implement block, in order: `check-package-baselines`, `check-package-smoke`, `check-package-autopkgtests`, `check-dependent-harness`.
      - Do not invent, rename, reorder, merge, or split phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or any YAML-source indirection.
      - Do not introduce agent-selected `bounce_targets`; every verifier has the fixed `bounce_target` declared in this workflow.

      Preexisting Inputs
      Consume or update these existing local artifacts in place. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `safe/crates/girepository/`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/stage-package-tree.py`
      - `safe/abi/link-compat/girepository.json`
      - `safe/debian/*`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/installed-files.json`
      - `safe/abi/postinst-state/runtime.json`
      - `safe/abi/debian-control-preservation.json`
      - `safe/tests/package/*`
      - `safe/debian/tests/*`
      - `dependents.json`
      - `test-original.sh`

      New Outputs
      Produce the phase outputs below, plus one git commit containing this phase's work before yielding. Do not leave required deliverables only in the worktree.
      - Safe Debian packages that match the recorded install manifests and trigger behavior.
      - A converted dependent harness that installs and exercises the safe packages rather than the original prefix build, including explicit `package-smoke`, `debian-tests`, `dependents`, and `all` scopes.

      Artifact Handling Rules
      - Work from the current repository root and follow `AGENTS.md`.
      - Preserve unrelated user work; only edit files needed for this phase.
      - Consume or update existing artifacts instead of replacing them with fresh upstream fetches or newly rediscovered inventories.
      - If a listed artifact is stale, update it in place only when the phase source explicitly requires that change.
      - Commit the completed phase work to git before yielding to the verifier phases.

      Phase Source
      # Integrate Debian Packaging and Convert the Dependent Harness

      ## Phase Name
      Integrate Debian packaging and convert the dependent harness

      ## Implement Phase ID
      `impl-package-integration`

      ## Source Plan Context
      - Overall target remains GLib 2.80.0 package compatibility on Ubuntu 24.04, after the library crates have stopped relying on shipped upstream object payloads.
      - `safe/debian/tests/control` already declares the Debian package test surface: `build`, `build-static`, `installed-tests`, `closure-refcount`, `debugcontroller`, `gdbus-server-auth`, `gdbus-threading`, `gmenumodel`, `mainloop`, `memory-monitor-dbus`, `socket`, `testfilemonitor`, `thread-pool-slow`, `threadtests`, `timeout`, `timer`, and `1065022-futureproofing`.
      - Current harness state: `test-original.sh` builds and installs upstream GLib into `/opt/glib-original`, asserts binaries load the original library, installs dependents, and runs package-specific runtime probes. It does not yet build, install, or exercise the safe Debian packages.
      - The package-content contracts already exist in `safe/abi/install-manifests/*.json`, `safe/abi/installed-files.json`, `safe/abi/postinst-state/runtime.json`, and `safe/abi/debian-control-preservation.json`; consume and update them in place.
      - `dependents.json` is the canonical dependent inventory and must remain the source of truth for dependent coverage, including the compile-time-only `budgie-artwork` / `pocillo-icon-theme` path.

      ## Preexisting Inputs
      - `safe/crates/girepository/`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/stage-package-tree.py`
      - `safe/abi/link-compat/girepository.json`
      - `safe/debian/*`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/installed-files.json`
      - `safe/abi/postinst-state/runtime.json`
      - `safe/abi/debian-control-preservation.json`
      - `safe/tests/package/*`
      - `safe/debian/tests/*`
      - `dependents.json`
      - `test-original.sh`

      ## New Outputs
      - Safe Debian packages that match the recorded install manifests and trigger behavior.
      - A converted dependent harness that installs and exercises the safe packages rather than the original prefix build, including explicit `package-smoke`, `debian-tests`, `dependents`, and `all` scopes.

      ## File Changes
      - `test-original.sh`
      - `safe/debian/control`
      - `safe/debian/rules`
      - `safe/debian/tests/control`
      - `safe/debian/tests/{build,build-static,installed-tests,closure-refcount,debugcontroller,gdbus-server-auth,gdbus-threading,gmenumodel,mainloop,memory-monitor-dbus,run-with-locales,socket,testfilemonitor,thread-pool-slow,threadtests,timeout,timer,1065022-futureproofing}`
      - `safe/tests/package/girepository-compile-only.sh`
      - `safe/tests/package/girepository-installed.sh`
      - `safe/tools/stage-package-tree.py`
      - `safe/tools/compare-debian-control.py`
      - `safe/tools/verify-package-baselines.py`
      - `safe/tools/compare-installed-files.py`
      - `safe/tools/check-postinst-state.py`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/installed-files.json`
      - `safe/abi/postinst-state/runtime.json`
      - `safe/abi/debian-control-preservation.json`

      ## Implementation Details
      - Consume `safe/debian/*`, `safe/tests/package/*`, `safe/debian/tests/*`, `dependents.json`, and `test-original.sh` in place; do not replace them with a separate generated harness.
      - Replace the original-only harness logic in `test-original.sh`: `assert_binary_uses_original_glib()` at line 60, `build_original_glib()` at line 119, `install_runtime_packages()` at line 152, `run_manifest_entry()` at line 481, and `main()` at line 539. Replace it with a safe-package path that builds the Debian packages from `safe/`, installs them in the container, asserts binaries resolve GLib from the installed safe package path, and reuses the existing dependent runtime probes.
      - In safe mode, copy `safe/` into a writable container path before building, because the repository is mounted read-only in the container. Install the build dependencies declared by `safe/debian/control` before invoking `dpkg-buildpackage`; do not reuse the original-tree Meson install as a proxy for the safe package build.
      - Keep an original-mode path only if it is useful as an oracle, but safe mode must be the default from this phase onward.
      - Add a harness mode variable such as `GLIB_UNDER_TEST=original|safe` and a scope variable such as `GLIB_TEST_SCOPE=package-smoke|debian-tests|dependents|all` so later checks can select the right package/runtime coverage without maintaining multiple scripts.
      - Define `package-smoke` so it builds and installs the safe packages, then runs `safe/debian/tests/build`, `safe/debian/tests/build-static`, `safe/tests/package/girepository-compile-only.sh`, and `safe/tests/package/girepository-installed.sh`.
      - Define `debian-tests` so it performs the same safe-package build/install setup needed for the current harness run, then executes every Debian autopkgtest entry point declared in `safe/debian/tests/control`: `installed-tests`, `closure-refcount`, `debugcontroller`, `gdbus-server-auth`, `gdbus-threading`, `gmenumodel`, `mainloop`, `memory-monitor-dbus`, `socket`, `testfilemonitor`, `thread-pool-slow`, `threadtests`, `timeout`, `timer`, and `1065022-futureproofing`, invoking `safe/debian/tests/run-with-locales` wherever the test scripts already depend on it.
      - Define `dependents` so it installs the safe runtime packages, then runs the existing runtime probes for the `compile_time_and_runtime` dependents and the `budgie-artwork` source build that covers `pocillo-icon-theme`.
      - Define `all` so it runs `package-smoke`, `debian-tests`, and `dependents` in sequence.
      - In safe mode, remove the original prefix-style `set_glib_env()` override model. The checks must prove that the installed package files on the container filesystem are what the binaries and build tools actually resolve.
      - Make the package test scripts repository-relative and container-safe instead of assuming a fixed `/src/...` path.
      - Preserve the `verify_manifest()` exact dependent inventory check in `test-original.sh`; only update that assertion if `dependents.json` itself changes.
      - Keep the package content, triggers, and `debian/control` structure aligned with `safe/abi/install-manifests/*.json`, `safe/abi/postinst-state/runtime.json`, and `safe/abi/debian-control-preservation.json`.
      - Critical file responsibilities owned by this phase:
        - `safe/debian/*` is the Debian source package, install lists, autopkgtests, triggers, and helper-script surface for Ubuntu drop-in replacement.
        - `safe/tools/stage-package-tree.py` maps the build root into the Debian package filesystem layout and must stop resolving package payloads through `safe/vendor/build-check`.
        - `safe/tools/verify-package-baselines.py`, `safe/tools/compare-debian-control.py`, `safe/tools/compare-installed-files.py`, and `safe/tools/check-postinst-state.py` are the package, installed-file, control-preservation, and trigger-state verification tools.
        - `safe/abi/install-manifests/*.json`, `safe/abi/installed-files.json`, `safe/abi/postinst-state/runtime.json`, and `safe/abi/debian-control-preservation.json` are package-content and trigger-state contracts consumed and updated only in place.
        - `safe/tests/package/*` are package-level compile/install smoke tests that must run against installed safe packages, not host distro GLib.
        - `test-original.sh` becomes the safe-package harness while preserving dependent inventory coverage and explicit `package-smoke`, `debian-tests`, `dependents`, and `all` scopes.
        - `dependents.json` remains the canonical dependent inventory.

      ## Verification Phases
      ### `check-package-baselines`
      - Phase ID: `check-package-baselines`
      - Type: `check`
      - Fixed `bounce_target`: `impl-package-integration`
      - Purpose: Verify the built packages, `debian/control` preservation contract, installed-file manifests, and postinst/postrm trigger behavior.
      - Commands:
      ```bash
      cd safe
      dpkg-buildpackage -b -uc -us
      python3 tools/compare-debian-control.py --baseline abi/debian-control-preservation.json --control debian/control
      python3 tools/verify-package-baselines.py --source . --work-root build-package-baselines --abi-shell-profiles "nodoc noinsttest nogir noudeb" --install-manifests abi/install-manifests --postinst-manifest abi/postinst-state/runtime.json
      ```

      ### `check-package-smoke`
      - Phase ID: `check-package-smoke`
      - Type: `check`
      - Fixed `bounce_target`: `impl-package-integration`
      - Purpose: Verify compile-only and installed-package developer workflows against packages actually built and installed from `safe/`.
      - Commands:
      ```bash
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=package-smoke ./test-original.sh
      ```

      ### `check-package-autopkgtests`
      - Phase ID: `check-package-autopkgtests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-package-integration`
      - Purpose: Verify that the installed safe packages satisfy the Debian autopkgtest surface declared in `safe/debian/tests/control`, not just the compile/install smoke subset.
      - Commands:
      ```bash
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=debian-tests ./test-original.sh
      ```

      ### `check-dependent-harness`
      - Phase ID: `check-dependent-harness`
      - Type: `check`
      - Fixed `bounce_target`: `impl-package-integration`
      - Purpose: Prove that the safe packages are drop-in replacements for the dependent inventory in `dependents.json`, including the compile-time-only `budgie-artwork` / `pocillo-icon-theme` coverage path.
      - Commands:
      ```bash
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=dependents ./test-original.sh
      ```

      ## Success Criteria
      - `check-package-baselines`, `check-package-smoke`, `check-package-autopkgtests`, and `check-dependent-harness` all pass.
      - `test-original.sh` installs and exercises the safe packages in its default mode.

      ## Git Commit Requirement
      - The implementer must commit work to git before yielding.
      - This phase must produce at least one new git commit before yielding to its verification phases.
      - A verifier must treat an unchanged `HEAD` or a worktree-only deliverable as a failure.
  - id: check-package-baselines
    type: check
    bounce_target: impl-package-integration
    prompt: |
      Reviewer role: You are the checker for phase `check-package-baselines`, reviewing the work produced by `impl-package-integration`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `22`.
      - Phase ID: `check-package-baselines`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-package-integration`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `safe/debian/*`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/installed-files.json`
      - `safe/abi/postinst-state/runtime.json`
      - `safe/abi/debian-control-preservation.json`
      - `safe/tools/compare-debian-control.py`
      - `safe/tools/verify-package-baselines.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-package-integration`.
      - Verify `impl-package-integration` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-package-integration` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-package-integration`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      dpkg-buildpackage -b -uc -us
      python3 tools/compare-debian-control.py --baseline abi/debian-control-preservation.json --control debian/control
      python3 tools/verify-package-baselines.py --source . --work-root build-package-baselines --abi-shell-profiles "nodoc noinsttest nogir noudeb" --install-manifests abi/install-manifests --postinst-manifest abi/postinst-state/runtime.json
      ```

      Phase Check Source
      ### `check-package-baselines`
      - Phase ID: `check-package-baselines`
      - Type: `check`
      - Fixed `bounce_target`: `impl-package-integration`
      - Purpose: Verify the built packages, `debian/control` preservation contract, installed-file manifests, and postinst/postrm trigger behavior.
      - Commands:
      ```bash
      cd safe
      dpkg-buildpackage -b -uc -us
      python3 tools/compare-debian-control.py --baseline abi/debian-control-preservation.json --control debian/control
      python3 tools/verify-package-baselines.py --source . --work-root build-package-baselines --abi-shell-profiles "nodoc noinsttest nogir noudeb" --install-manifests abi/install-manifests --postinst-manifest abi/postinst-state/runtime.json
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-package-smoke
    type: check
    bounce_target: impl-package-integration
    prompt: |
      Reviewer role: You are the checker for phase `check-package-smoke`, reviewing the work produced by `impl-package-integration`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `23`.
      - Phase ID: `check-package-smoke`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-package-integration`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `test-original.sh`
      - `safe/debian/tests/*`
      - `safe/tests/package/*`
      - `safe/debian/control`
      - `safe/debian/rules`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-package-integration`.
      - Verify `impl-package-integration` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-package-integration` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-package-integration`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=package-smoke ./test-original.sh
      ```

      Phase Check Source
      ### `check-package-smoke`
      - Phase ID: `check-package-smoke`
      - Type: `check`
      - Fixed `bounce_target`: `impl-package-integration`
      - Purpose: Verify compile-only and installed-package developer workflows against packages actually built and installed from `safe/`.
      - Commands:
      ```bash
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=package-smoke ./test-original.sh
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-package-autopkgtests
    type: check
    bounce_target: impl-package-integration
    prompt: |
      Reviewer role: You are the checker for phase `check-package-autopkgtests`, reviewing the work produced by `impl-package-integration`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `24`.
      - Phase ID: `check-package-autopkgtests`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-package-integration`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `test-original.sh`
      - `safe/debian/tests/*`
      - `safe/debian/tests/control`
      - `safe/debian/control`
      - `safe/debian/rules`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-package-integration`.
      - Verify `impl-package-integration` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-package-integration` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-package-integration`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=debian-tests ./test-original.sh
      ```

      Phase Check Source
      ### `check-package-autopkgtests`
      - Phase ID: `check-package-autopkgtests`
      - Type: `check`
      - Fixed `bounce_target`: `impl-package-integration`
      - Purpose: Verify that the installed safe packages satisfy the Debian autopkgtest surface declared in `safe/debian/tests/control`, not just the compile/install smoke subset.
      - Commands:
      ```bash
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=debian-tests ./test-original.sh
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-dependent-harness
    type: check
    bounce_target: impl-package-integration
    prompt: |
      Reviewer role: You are the checker for phase `check-dependent-harness`, reviewing the work produced by `impl-package-integration`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `25`.
      - Phase ID: `check-dependent-harness`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-package-integration`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `dependents.json`
      - `test-original.sh`
      - `safe/debian/control`
      - `safe/debian/rules`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-package-integration`.
      - Verify `impl-package-integration` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-package-integration` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-package-integration`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=dependents ./test-original.sh
      ```

      Phase Check Source
      ### `check-dependent-harness`
      - Phase ID: `check-dependent-harness`
      - Type: `check`
      - Fixed `bounce_target`: `impl-package-integration`
      - Purpose: Prove that the safe packages are drop-in replacements for the dependent inventory in `dependents.json`, including the compile-time-only `budgie-artwork` / `pocillo-icon-theme` coverage path.
      - Commands:
      ```bash
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=dependents ./test-original.sh
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: impl-final-fixups
    type: implement
    bounce_target: null
    prompt: |
      Role: Implementer for phase `impl-final-fixups`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `26`.
      - Phase ID: `impl-final-fixups`.
      - Phase type: `implement`.
      - Fixed top-level `bounce_target`: `null`.
      - Verifier phases that immediately follow this implement block, in order: `check-final-full`, `check-final-safety`.
      - Do not invent, rename, reorder, merge, or split phases.
      - Do not introduce top-level `include`, `parallel_groups`, phase-level `prompt_file`, `workflow_file`, `workflow_dir`, `checks`, or any YAML-source indirection.
      - Do not introduce agent-selected `bounce_targets`; every verifier has the fixed `bounce_target` declared in this workflow.

      Preexisting Inputs
      Consume or update these existing local artifacts in place. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `test-original.sh`
      - `original/`
      - `safe/debian/`
      - `safe/vendor/original/`
      - `safe/vendor/build-check/`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/installed-files.json`
      - `safe/abi/postinst-state/runtime.json`
      - `safe/abi/debian-control-preservation.json`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/abi/link-compat/*.json`
      - `safe/abi/layout-manifests/*.json`
      - `safe/abi/layouts/*.json`
      - `safe/abi/symbols/*.symbols`
      - `safe/abi/version-scripts/*.map`
      - `safe/abi/debian-patches.json`
      - `safe/tests/upstream/`
      - `safe/tests/manifests/full.txt`
      - `safe/tests/cve/*`
      - `safe/tests/package/*`
      - `relevant_cves.json`
      - `dependents.json`
      - `safe/crates/`
      - `safe/Cargo.toml`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/check-unsafe-audit.py`
      - `safe/tools/compare-layouts.py`
      - `safe/tools/compare-symbols.py`
      - `safe/tools/stage-package-tree.py`
      - `safe/tools/compare-debian-control.py`
      - `safe/tools/verify-package-baselines.py`
      - `safe/tools/extract_abi.py`
      - `safe/tools/extract_layouts.py`
      - `safe/tools/link-compat.py`
      - `safe/tools/run-meson-manifest.py`
      - `safe/tools/run-cve-regressions.py`
      - `safe/tools/verify-debian-patches.py`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/debian-patch-provenance.md`

      New Outputs
      Produce the phase outputs below, plus one git commit containing this phase's work before yielding. Do not leave required deliverables only in the worktree.
      - Final release-ready safe workspace and package set.
      - Final safety audit contract.
      - Final docs synchronized with the code and packaging state.

      Artifact Handling Rules
      - Work from the current repository root and follow `AGENTS.md`.
      - Preserve unrelated user work; only edit files needed for this phase.
      - Consume or update existing artifacts instead of replacing them with fresh upstream fetches or newly rediscovered inventories.
      - If a listed artifact is stale, update it in place only when the phase source explicitly requires that change.
      - Commit the completed phase work to git before yielding to the verifier phases.

      Phase Source
      # Final Hardening, Unsafe Reduction, and Full Verification

      ## Phase Name
      Final hardening, unsafe reduction, and full verification

      ## Implement Phase ID
      `impl-final-fixups`

      ## Source Plan Context
      - Overall target is the completed GLib 2.80.0 Rust implementation in `safe/`, source-compatible, link-compatible, runtime-compatible, and package-compatible on Ubuntu 24.04.
      - Final export counts remain the authoritative target: `libglib-2.0.so.0` 1872 symbols, `libgthread-2.0.so.0` 2, `libgmodule-2.0.so.0` 10, `libgobject-2.0.so.0` 478, `libgio-2.0.so.0` 2107, and `libgirepository-2.0.so.0` 231.
      - Final frozen manifest target: `safe/tests/manifests/full.txt` has 384 rows, and the final CVE run covers all entries represented by `relevant_cves.json` and `safe/tests/cve/*`.
      - This phase is the catch-all for residual interoperability, package, and safety breakage only after phases 1-7 have moved shipped runtime, static archives, helper executables, and tools away from `safe/vendor/build-check`.
      - The final implementation is incomplete if any shipped library still depends on linked upstream object files, any shipped static archive or helper executable still comes from `safe/vendor/build-check`, any GIRepository placeholder export remains, safe packages fail the package/debian/dependent harness scopes, or avoidable unsafe code remains.

      ## Preexisting Inputs
      - `test-original.sh`
      - `original/`
      - `safe/debian/`
      - `safe/vendor/original/`
      - `safe/vendor/build-check/`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/installed-files.json`
      - `safe/abi/postinst-state/runtime.json`
      - `safe/abi/debian-control-preservation.json`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/abi/link-compat/*.json`
      - `safe/abi/layout-manifests/*.json`
      - `safe/abi/layouts/*.json`
      - `safe/abi/symbols/*.symbols`
      - `safe/abi/version-scripts/*.map`
      - `safe/abi/debian-patches.json`
      - `safe/tests/upstream/`
      - `safe/tests/manifests/full.txt`
      - `safe/tests/cve/*`
      - `safe/tests/package/*`
      - `relevant_cves.json`
      - `dependents.json`
      - `safe/crates/`
      - `safe/Cargo.toml`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/check-unsafe-audit.py`
      - `safe/tools/compare-layouts.py`
      - `safe/tools/compare-symbols.py`
      - `safe/tools/stage-package-tree.py`
      - `safe/tools/compare-debian-control.py`
      - `safe/tools/verify-package-baselines.py`
      - `safe/tools/extract_abi.py`
      - `safe/tools/extract_layouts.py`
      - `safe/tools/link-compat.py`
      - `safe/tools/run-meson-manifest.py`
      - `safe/tools/run-cve-regressions.py`
      - `safe/tools/verify-debian-patches.py`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/debian-patch-provenance.md`

      ## New Outputs
      - Final release-ready safe workspace and package set.
      - Final safety audit contract.
      - Final docs synchronized with the code and packaging state.

      ## File Changes
      - `safe/tools/check-unsafe-audit.py`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/debian-patch-provenance.md`
      - Targeted residual fixes in the minimum earlier-phase owner file that still violates `check-final-full` or `check-final-safety`; do not reopen earlier phase scope wholesale.

      ## Implementation Details
      - Remove or justify every remaining `unsafe` block. The final audit should allow only required FFI/OS boundary unsafe code and should fail on undocumented or avoidable unsafe.
      - Ensure no crate `build.rs` still injects or references vendored payloads from `safe/vendor/build-check` or the GLib backend replay tool in the final cdylibs.
      - Ensure no shipped static archive or installed helper executable is still copied from `safe/vendor/build-check`; this explicitly includes `safe/tools/stage-package-tree.py`, whose package payload resolution must come from the current build root or source tree instead. Keeping upstream script-template packaging from `safe/vendor/original/` is acceptable where the shipped interface is already Python or shell.
      - Eliminate any remaining placeholder exports, dead bootstrap forwarders, or test-only compatibility shims that would make the installed package depend on the original C implementation.
      - Confirm `safe/crates/abi-support/src/ffi.rs` and `safe/crates/abi-support/src/bin/layout-probe.rs` were extended only alongside public ABI types that became Rust-owned, and that final layout verification exercises those shared definitions.
      - Refresh the CVE matrix and Debian patch provenance documentation to describe the final Rust-owned behavior, not the bootstrap shell.
      - Final verification contract owned by this phase:
        - Metadata and contract verification must run `cargo check --workspace`, ABI extraction verification, layout extraction verification, link-compat manifest verification, and Debian patch manifest verification.
        - Build-root verification must build `build-final`, run the `full` link-compat phase, replay `tests/manifests/full.txt`, run all CVE regressions, and compare all recorded public layouts.
        - Exported-symbol verification must compare every final library in `build-final` against `safe/abi/symbols/*.symbols`.
        - Package verification must build the Debian packages and run the control-preservation and package-baseline checks.
        - Installed-package verification must run `GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=all ./test-original.sh` from the repository root, represented in the checker as `../test-original.sh` after `cd safe`.
        - Final cleanup verification must reject remaining placeholder exports, vendored build-check references in crate build scripts or package staging, GLib backend replay linkage, vendored static-archive fallback keys in `tools/build-abi-shell.py`, and avoidable unsafe code.
      - Critical file responsibilities owned by this phase:
        - `safe/tools/check-unsafe-audit.py` is the final safety gate and must allow only justified FFI/OS-boundary unsafe code.
        - `safe/abi/symbols/*.symbols` are the Debian symbol files and final symbol-surface verification baselines.
        - `safe/docs/debian-patch-provenance.md` records how Debian/Ubuntu patch behavior is preserved or absorbed into the safe source.

      ## Verification Phases
      ### `check-final-full`
      - Phase ID: `check-final-full`
      - Type: `check`
      - Fixed `bounce_target`: `impl-final-fixups`
      - Purpose: Run the full metadata, build-root interoperability, CVE, package, and installed-package harness checks against one final build root.
      - Commands:
      ```bash
      cd safe
      cargo check --workspace
      python3 tools/extract_abi.py --verify
      python3 tools/extract_layouts.py --verify
      python3 tools/link-compat.py --verify-manifests
      python3 tools/verify-debian-patches.py --verify-manifest
      python3 tools/build-abi-shell.py --build-root build-final --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-final/.stamp
      python3 tools/link-compat.py --phase full --build-root build-final --compile-original-objects --run
      python3 tools/run-meson-manifest.py --build-root build-final --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-final/meson-info/intro-tests.json --manifest tests/manifests/full.txt --print-errorlogs
      python3 tools/run-cve-regressions.py --all --build-root build-final --rebuild
      dpkg-buildpackage -b -uc -us
      python3 tools/compare-debian-control.py --baseline abi/debian-control-preservation.json --control debian/control
      python3 tools/verify-package-baselines.py --source . --work-root build-package-baselines-final --abi-shell-profiles "nodoc noinsttest nogir noudeb" --install-manifests abi/install-manifests --postinst-manifest abi/postinst-state/runtime.json
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=all ../test-original.sh
      ```

      ### `check-final-safety`
      - Phase ID: `check-final-safety`
      - Type: `check`
      - Fixed `bounce_target`: `impl-final-fixups`
      - Purpose: Enforce the final safety, symbol/layout parity, and "no bootstrap leftovers" contract.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-final --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-final/.stamp
      python3 tools/check-unsafe-audit.py
      ! rg -n "PLACEHOLDER_" crates/girepository/src/exports.rs
      ! rg -n "vendor/build-check" crates/*/build.rs
      ! rg -n "build-glib-backend.py" crates/glib/build.rs
      ! rg -n "SAFE_VENDOR_BUILD_CHECK|vendor/build-check" tools/stage-package-tree.py
      ! rg -n "static_objects|static_archives|build_vendored_static_archive" tools/build-abi-shell.py
      python3 tools/compare-layouts.py --build-root build-final --baseline abi/layouts/glib.json --baseline abi/layouts/gthread.json --baseline abi/layouts/gmodule.json --baseline abi/layouts/gobject.json --baseline abi/layouts/gio.json --baseline abi/layouts/girepository.json
      for pair in \
        "abi/symbols/libglib-2.0.so.0.symbols build-final/glib/libglib-2.0.so.0.8000.0" \
        "abi/symbols/libgthread-2.0.so.0.symbols build-final/gthread/libgthread-2.0.so.0.8000.0" \
        "abi/symbols/libgmodule-2.0.so.0.symbols build-final/gmodule/libgmodule-2.0.so.0.8000.0" \
        "abi/symbols/libgobject-2.0.so.0.symbols build-final/gobject/libgobject-2.0.so.0.8000.0" \
        "abi/symbols/libgio-2.0.so.0.symbols build-final/gio/libgio-2.0.so.0.8000.0" \
        "abi/symbols/libgirepository-2.0.so.0.symbols build-final/girepository/libgirepository-2.0.so.0.8000.0"; do \
          set -- $pair; \
          python3 tools/compare-symbols.py --expected "$1" --library "$2"; \
        done
      ```

      ## Success Criteria
      - `check-final-full` and `check-final-safety` both pass.
      - The workspace has no remaining interoperability, package, or safety breakage that requires another phase-specific bounce target.
      - The implementation is not complete if any library still depends on linked upstream object files for its shipped runtime, any shipped static archive or helper executable still comes from `safe/vendor/build-check`, any placeholder export remains, the safe packages fail `package-smoke`, `debian-tests`, or `dependents`, or the final unsafe audit flags avoidable unsafe code.

      ## Git Commit Requirement
      - The implementer must commit work to git before yielding.
      - This phase must produce at least one new git commit before yielding to its verification phases.
      - A verifier must treat an unchanged `HEAD` or a worktree-only deliverable as a failure.
  - id: check-final-full
    type: check
    bounce_target: impl-final-fixups
    prompt: |
      Reviewer role: You are the checker for phase `check-final-full`, reviewing the work produced by `impl-final-fixups`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `27`.
      - Phase ID: `check-final-full`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-final-fixups`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `test-original.sh`
      - `original/`
      - `dependents.json`
      - `relevant_cves.json`
      - `safe/Cargo.toml`
      - `safe/debian/`
      - `safe/vendor/original/`
      - `safe/vendor/build-check/`
      - `safe/abi/tests.json`
      - `safe/abi/test-source-path-map.json`
      - `safe/abi/link-compat/*.json`
      - `safe/abi/layout-manifests/*.json`
      - `safe/abi/layouts/*.json`
      - `safe/abi/version-scripts/*.map`
      - `safe/abi/debian-patches.json`
      - `safe/abi/install-manifests/*.json`
      - `safe/abi/postinst-state/runtime.json`
      - `safe/abi/debian-control-preservation.json`
      - `safe/tests/manifests/full.txt`
      - `safe/tests/cve/*`
      - `safe/tests/package/*`
      - `safe/tests/upstream/`
      - `safe/tools/extract_abi.py`
      - `safe/tools/extract_layouts.py`
      - `safe/tools/link-compat.py`
      - `safe/tools/verify-debian-patches.py`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/run-meson-manifest.py`
      - `safe/tools/run-cve-regressions.py`
      - `safe/tools/compare-debian-control.py`
      - `safe/tools/verify-package-baselines.py`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-final-fixups`.
      - Verify `impl-final-fixups` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-final-fixups` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-final-fixups`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      cargo check --workspace
      python3 tools/extract_abi.py --verify
      python3 tools/extract_layouts.py --verify
      python3 tools/link-compat.py --verify-manifests
      python3 tools/verify-debian-patches.py --verify-manifest
      python3 tools/build-abi-shell.py --build-root build-final --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-final/.stamp
      python3 tools/link-compat.py --phase full --build-root build-final --compile-original-objects --run
      python3 tools/run-meson-manifest.py --build-root build-final --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-final/meson-info/intro-tests.json --manifest tests/manifests/full.txt --print-errorlogs
      python3 tools/run-cve-regressions.py --all --build-root build-final --rebuild
      dpkg-buildpackage -b -uc -us
      python3 tools/compare-debian-control.py --baseline abi/debian-control-preservation.json --control debian/control
      python3 tools/verify-package-baselines.py --source . --work-root build-package-baselines-final --abi-shell-profiles "nodoc noinsttest nogir noudeb" --install-manifests abi/install-manifests --postinst-manifest abi/postinst-state/runtime.json
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=all ../test-original.sh
      ```

      Phase Check Source
      ### `check-final-full`
      - Phase ID: `check-final-full`
      - Type: `check`
      - Fixed `bounce_target`: `impl-final-fixups`
      - Purpose: Run the full metadata, build-root interoperability, CVE, package, and installed-package harness checks against one final build root.
      - Commands:
      ```bash
      cd safe
      cargo check --workspace
      python3 tools/extract_abi.py --verify
      python3 tools/extract_layouts.py --verify
      python3 tools/link-compat.py --verify-manifests
      python3 tools/verify-debian-patches.py --verify-manifest
      python3 tools/build-abi-shell.py --build-root build-final --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-final/.stamp
      python3 tools/link-compat.py --phase full --build-root build-final --compile-original-objects --run
      python3 tools/run-meson-manifest.py --build-root build-final --baseline abi/tests.json --path-map abi/test-source-path-map.json --intro-tests build-final/meson-info/intro-tests.json --manifest tests/manifests/full.txt --print-errorlogs
      python3 tools/run-cve-regressions.py --all --build-root build-final --rebuild
      dpkg-buildpackage -b -uc -us
      python3 tools/compare-debian-control.py --baseline abi/debian-control-preservation.json --control debian/control
      python3 tools/verify-package-baselines.py --source . --work-root build-package-baselines-final --abi-shell-profiles "nodoc noinsttest nogir noudeb" --install-manifests abi/install-manifests --postinst-manifest abi/postinst-state/runtime.json
      GLIB_UNDER_TEST=safe GLIB_TEST_SCOPE=all ../test-original.sh
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
  - id: check-final-safety
    type: check
    bounce_target: impl-final-fixups
    prompt: |
      Reviewer role: You are the checker for phase `check-final-safety`, reviewing the work produced by `impl-final-fixups`.

      Workflow Goal
      {{ GOAL }}

      Topology Contract
      - Phase order: `28`.
      - Phase ID: `check-final-safety`.
      - Phase type: `check`.
      - Fixed top-level `bounce_target`: `impl-final-fixups`.
      - This verifier must remain immediately after the implement block it verifies, in the order declared by `.plan/workflow-structure.yaml`.
      - Do not invent alternate bounce targets, skip this verifier, or delegate verification to phase-level `checks`.

      Preexisting Inputs
      Use these existing artifacts for review. Do not refetch, recollect, rediscover, resync, or regenerate checked-in inventories, upstream snapshots, CVE data, dependent data, ABI manifests, package manifests, or upstream test manifests from scratch.
      - `safe/crates/`
      - `safe/Cargo.toml`
      - `safe/tools/build-abi-shell.py`
      - `safe/tools/check-unsafe-audit.py`
      - `safe/tools/compare-layouts.py`
      - `safe/tools/compare-symbols.py`
      - `safe/tools/stage-package-tree.py`
      - `safe/abi/layouts/*.json`
      - `safe/abi/symbols/*.symbols`
      - `safe/crates/abi-support/src/ffi.rs`
      - `safe/crates/abi-support/src/bin/layout-probe.rs`
      - `safe/docs/cve-matrix.md`
      - `safe/docs/debian-patch-provenance.md`

      Specific Things To Verify
      - Verify the implementation under review is the fixed bounce target `impl-final-fixups`.
      - Verify `impl-final-fixups` produced at least one git commit before yielding; treat an unchanged `HEAD` or worktree-only deliverables as a failure.
      - Verify the required local artifacts and source changes for `impl-final-fixups` are present and internally consistent.
      - Run the git/worktree commands and phase-specific commands listed below from the repository root unless the command block itself changes directory.
      - Review command failures, test failures, packaging failures, missing artifacts, placeholder implementations, vendored bootstrap leftovers, or uncommitted required deliverables as failures.
      - Preserve generated build/test evidence, but do not make source changes as a checker unless the workflow bounce mechanism explicitly returns to `impl-final-fixups`.

      Git/Worktree Commands To Run
      ```bash
      git status --short
      git log -1 --oneline
      ```

      Phase Commands To Run
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-final --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-final/.stamp
      python3 tools/check-unsafe-audit.py
      ! rg -n "PLACEHOLDER_" crates/girepository/src/exports.rs
      ! rg -n "vendor/build-check" crates/*/build.rs
      ! rg -n "build-glib-backend.py" crates/glib/build.rs
      ! rg -n "SAFE_VENDOR_BUILD_CHECK|vendor/build-check" tools/stage-package-tree.py
      ! rg -n "static_objects|static_archives|build_vendored_static_archive" tools/build-abi-shell.py
      python3 tools/compare-layouts.py --build-root build-final --baseline abi/layouts/glib.json --baseline abi/layouts/gthread.json --baseline abi/layouts/gmodule.json --baseline abi/layouts/gobject.json --baseline abi/layouts/gio.json --baseline abi/layouts/girepository.json
      for pair in \
        "abi/symbols/libglib-2.0.so.0.symbols build-final/glib/libglib-2.0.so.0.8000.0" \
        "abi/symbols/libgthread-2.0.so.0.symbols build-final/gthread/libgthread-2.0.so.0.8000.0" \
        "abi/symbols/libgmodule-2.0.so.0.symbols build-final/gmodule/libgmodule-2.0.so.0.8000.0" \
        "abi/symbols/libgobject-2.0.so.0.symbols build-final/gobject/libgobject-2.0.so.0.8000.0" \
        "abi/symbols/libgio-2.0.so.0.symbols build-final/gio/libgio-2.0.so.0.8000.0" \
        "abi/symbols/libgirepository-2.0.so.0.symbols build-final/girepository/libgirepository-2.0.so.0.8000.0"; do \
          set -- $pair; \
          python3 tools/compare-symbols.py --expected "$1" --library "$2"; \
        done
      ```

      Phase Check Source
      ### `check-final-safety`
      - Phase ID: `check-final-safety`
      - Type: `check`
      - Fixed `bounce_target`: `impl-final-fixups`
      - Purpose: Enforce the final safety, symbol/layout parity, and "no bootstrap leftovers" contract.
      - Commands:
      ```bash
      cd safe
      python3 tools/build-abi-shell.py --build-root build-final --multiarch "$(dpkg-architecture -qDEB_HOST_MULTIARCH)" --stamp build-final/.stamp
      python3 tools/check-unsafe-audit.py
      ! rg -n "PLACEHOLDER_" crates/girepository/src/exports.rs
      ! rg -n "vendor/build-check" crates/*/build.rs
      ! rg -n "build-glib-backend.py" crates/glib/build.rs
      ! rg -n "SAFE_VENDOR_BUILD_CHECK|vendor/build-check" tools/stage-package-tree.py
      ! rg -n "static_objects|static_archives|build_vendored_static_archive" tools/build-abi-shell.py
      python3 tools/compare-layouts.py --build-root build-final --baseline abi/layouts/glib.json --baseline abi/layouts/gthread.json --baseline abi/layouts/gmodule.json --baseline abi/layouts/gobject.json --baseline abi/layouts/gio.json --baseline abi/layouts/girepository.json
      for pair in \
        "abi/symbols/libglib-2.0.so.0.symbols build-final/glib/libglib-2.0.so.0.8000.0" \
        "abi/symbols/libgthread-2.0.so.0.symbols build-final/gthread/libgthread-2.0.so.0.8000.0" \
        "abi/symbols/libgmodule-2.0.so.0.symbols build-final/gmodule/libgmodule-2.0.so.0.8000.0" \
        "abi/symbols/libgobject-2.0.so.0.symbols build-final/gobject/libgobject-2.0.so.0.8000.0" \
        "abi/symbols/libgio-2.0.so.0.symbols build-final/gio/libgio-2.0.so.0.8000.0" \
        "abi/symbols/libgirepository-2.0.so.0.symbols build-final/girepository/libgirepository-2.0.so.0.8000.0"; do \
          set -- $pair; \
          python3 tools/compare-symbols.py --expected "$1" --library "$2"; \
        done
      ```

      End your response with exactly one of:
      VERDICT: PASS
      VERDICT: FAIL: <reason>
